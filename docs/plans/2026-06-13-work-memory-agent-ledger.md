# x-tools Work Memory Agent 实现台账

来源: `docs/plans/2026-06-13-work-memory-agent-requirements.md`

实现口径: 本次落地为本机可验收 MVP。工作记忆、屏幕时间机器、时间线、搜索、草稿、建议、任务包、安全控制和降级提示必须可用；外部 AI、embedding、Milvus、OpsCore 同步默认不外发，只提供配置状态、证据草稿和用户确认边界。

> Ariadne Wails3 校准（2026-06-14）：本文件保留的是旧 x-tools/Python MVP 的工作记忆验收口径，不等同于 Ariadne 当前已完成状态。Ariadne 当前事实以 `docs/plans/2026-06-13-ariadne-implementation-ledger.md` 的 `0.0 开工状态卡` 和 `完整重构差距` 为准。
>
> 当前 Ariadne 已补：工作记忆时间线、搜索、截图证据、手动补记、手动笔记、敏感标记、删除当前记忆、清理未收藏记忆、可读 ZIP 导出、日报草稿、知识草稿和外部代理任务包。
>
> 当前 Ariadne 仍缺：自动 OCR 策略、SQLite FTS、embedding、Milvus、AI 日报/复盘、经验发现、导入旧包/Markdown/图片、旧历史迁移和稳定桌面点击验收。行级 OCR 文本选择和 OCR 图片叠框选择已在 Ariadne 截图、剪贴板和工作记忆 OCR 结果中接入。

## 1. 核心数据与隐私边界

- [x] 建立统一工作记忆数据模型，覆盖截图、剪贴板、笔记、文件、窗口、动作、日报、知识草稿、建议和任务包。
- [x] 记录时间、来源、摘要、上下文、标签、收藏、敏感、AI/向量化许可、证据关联等元数据。
- [x] 实现敏感内容识别，默认禁止敏感条目参与 AI 和 embedding。
- [x] 实现排除应用、进程、窗口标题、路径和隐私模式控制，且优先于采集、OCR、AI、导出和向量化。
- [x] 实现数据保留、存储上限、未收藏清理、时间段清理和可读数据包导出。

## 2. 采集与屏幕时间机器

- [x] 剪贴板文本、图片和文件路径进入工作记忆。
- [x] x-tools 主动截图和截图历史进入工作记忆。
- [x] 支持手动笔记、手动收藏搜索结果和导入文本/Markdown/图片。
- [x] 屏幕时间机器作为独立功能，用户开启后按间隔后台截图。
- [x] 支持多显示器全屏截图、缩略图、重复画面合并、保留策略和存储统计。
- [x] 自动截图状态可见，支持立即暂停、恢复、手动补记、删除当前记录、前后回看和跳转时间。
- [x] OCR 可按配置或用户操作执行，不可用时保留清晰降级原因。

## 3. 工作记忆中心与搜索

- [x] 提供工作记忆中心窗口，包含时间线、搜索、筛选、详情预览、证据、状态、控制区和操作按钮。
- [x] 支持按时间、来源、应用、标签、收藏、类型、日报/知识状态筛选。
- [x] 支持关键词搜索 OCR 文本、原文、文件名、路径、窗口标题、应用、标签、摘要、错误、IP、URL 等。
- [x] 支持自然语言/语义近似搜索的本地降级实现。
- [x] 搜索结果展示命中文本、截图/图片预览、来源、时间、关联证据和匹配原因。
- [x] 主搜索框支持 `mem`、`timeline`、`day`、`note`、`recall`、`trace`、`learn` 入口。

## 4. AI 草稿、经验发现与外部代理协作

- [x] 本地生成带证据引用的日报草稿。
- [x] 本地生成带证据引用的知识条目草稿。
- [x] 本地生成问题复盘草稿。
- [x] 从重复查询、来源和动作中生成候选工作流建议，默认不自动执行。
- [x] 生成 skill、工作流、检查清单、提示词和知识沉淀建议。
- [x] 生成外部代理任务包，包含目标、上下文、证据、边界、权限和验收标准。
- [x] AI/embedding/Milvus/OpsCore 不可用时，本地搜索、时间线和草稿仍可使用。

## 5. UI 入口、确认与用户操作

- [x] 托盘菜单提供工作记忆中心、屏幕时间机器暂停/恢复和手动补记入口。
- [x] 搜索结果和预览区提供加入记忆、查看证据、复制、收藏、敏感标记、生成日报/知识/任务包等类型化动作。
- [x] 删除、清空、导出、外发、同步、交给外部代理等中高风险操作保持确认边界。
- [x] 复制等轻量反馈保持在窗口内，不使用夸张系统通知。
- [x] 设置和中心页面提供采集开关、截图间隔、范围、质量、OCR/embedding/AI、排除规则和外部代理配置状态。

## 6. 验证

- [x] 补充核心管理器、搜索、敏感识别、草稿、导出和插件入口测试。
- [x] 运行相关 unittest 通过。
- [x] 运行应用并通过电脑操作技能实际打开工作记忆中心、开启时间机器、手动补记、搜索、生成草稿和查看状态。

验收记录:

- [x] `python -m unittest tests.core.test_work_memory tests.plugins.test_work_memory_plugin tests.ui.test_search_window_qr tests.core.test_capture_history -v` 通过。
- [x] 工作记忆中心 UI smoke 通过: 添加笔记、搜索、生成日报、生成外部任务包、保存配置均可执行。
- [x] 电脑操作技能验收通过: 实际打开 `工作记忆中心`，开启屏幕时间机器，执行手动补记，搜索 `gateway`，生成日报草稿、知识草稿和外部代理任务包，并确认状态栏、时间线和结果列表同步更新。
- [x] Computer Use 本地运行时存在 `@oai/sky` exports 缺失问题，已在本机运行时缓存补充 `./dist/*` 导出后完成验收。
