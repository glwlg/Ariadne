| Task | Status | Evidence |
| --- | --- | --- |
| 心流待办页截图反馈 P1 修正 | completed | 针对 2026-06-18 用户连续截图指出待办页仍不像设计稿：承认上一版仍不合格，并按 `C:\Users\luwei\AppData\Local\Temp\codex-clipboard-fb48d663-5c3d-416a-9567-d36dc173c4d5.png` 与 `C:\Users\luwei\AppData\Local\Temp\codex-clipboard-7696c409-cf3f-47db-9c58-97f6be2f47d8.png` 中可见问题修正。`FlowTodosPage.vue` 移除待办列表行内“开始/稍后/编辑/删除”动作组，避免按钮在窄列里被压成竖排；列表行只保留选择、标题、范围、时间、留痕和单个更多/编辑入口，处理动作保留在焦点卡。`style.css` 中待办页最终覆盖调整为更接近选中设计图：`.flow-todos-canvas` 上限放大到 `1540px` 并居中，`.flow-todos-board` 改为主栏自适应 + `360px` 提醒栏，`.flow-todos-focus-card` 拉高到 `400px` 并用明确 grid areas 分开 header/body/actions，列表 header 拉高到 `78px`、待办行拉高到 `92px`，右侧提醒栏按视口拉高到 `clamp(720px, calc(100vh - 150px), 920px)`，减少整页缩在左上角的观感。验证：`pnpm --dir frontend build`、`git diff --check`、`wails3 task windows:package` 通过；`rg -n "证据" frontend\src\components\workmemory\flow frontend\src\style.css` 无命中；Vite/Rolldown 仍有第三方 `@vueuse/core` pure annotation 警告，不影响产物；当前工具环境未暴露 Browser/Chrome 截图能力，`design-qa.md` 继续如实标记截图级 QA blocked。新 `bin\ariadne.exe` 为 `32970752` bytes，时间 `2026-06-18 23:01:24`，SHA256 `B38A2CD885C76BA0F3B124DD390B2D0C071DD7462DE23038428F03542B3A494E`；新 release zip 为 `17281655` bytes，时间 `2026-06-18 23:01:24`，SHA256 `7B4AFAE6030A414F3F087D5837F99C03B8A2EAF86D191D2BA221F93A186002F6`。 |
| 心流待办页按选中设计图重做 | completed | 针对 2026-06-18 用户指出“图 1 是设计图，图 2 是实际结果，不是一个东西”：承认上一版只是简化改版，未按源图还原；本轮按 `C:\Users\luwei\AppData\Local\Temp\codex-clipboard-9c836478-d426-4c0f-853a-43a1ec5f9582.png` 作为源图重做 `frontend/src/components/workmemory/flow/pages/FlowTodosPage.vue`，形成“待办”顶部标题、右侧搜索/新建/从心流整理、下一件事焦点卡、表格式“接下来”、折叠已完成、右侧提醒栏；重写待办页展示逻辑，清理长 note 中的字面 `\n`，将时间、范围、地点、优先级、留痕分位展示，避免后端字段整段铺开。`frontend/src/style.css` 追加待办页专用覆盖样式，限制主画布宽度、固定主栏/提醒栏比例、压缩列表行密度并匹配设计图按钮/卡片层级；`frontend/src/components/workmemory/flow/components/FlowCommandDock.vue` 在待办页显示 `待办总览` 和待办动作，避免泛用上下文 dock 破坏底部观感。更新 `design-qa.md`：源图已明确，当前仍 blocked 的唯一原因是本轮未暴露 Browser/Chrome 截图工具，按项目约束未使用 Playwright，因此不能声称截图级 QA 已通过。验证：Product Design user-context preflight 无保存上下文；`pnpm --dir frontend build` 通过；`rg -n "证据" frontend\src\components\workmemory\flow` 无命中；`git diff --check` 通过，仅有既有 CRLF 提示；`wails3 task windows:package` 通过，Vite/Rolldown 仍有第三方 `@vueuse/core` pure annotation 警告，不影响产物。新 `bin\ariadne.exe` 为 `32969728` bytes，时间 `2026-06-18 22:37:34`，SHA256 `737B1C152C530B11D9F89D162A7CF35B8973497FE5C504CDD20A7E1A8BEF0400`；新 release zip 为 `17285893` bytes，时间 `2026-06-18 22:37:35`，SHA256 `B05211EEFD350DCF9F9F6F722C595A19F8C1440A2B3E66701A11923F175B7F31`。 |
| 心流待办页专注模式改版 | completed | 针对 2026-06-18 用户选择 Product Design 方案 2“专注模式”，并明确要求待办页不要信息过载、不要纠结“证据”、心流内不要再出现“证据”一词：重写 `frontend/src/components/workmemory/flow/pages/FlowTodosPage.vue`，从原来的表单 + 统计卡 + 大分组列表改为“下一件事”焦点卡、轻量待办列表、折叠已完成和窄提醒栏；新增交互保持可用：新建、编辑、开始处理、完成、稍后、删除、刷新、从心流整理。重写 `frontend/src/style.css` 中待办页样式，去掉旧页面的大量空卡片、统计墙和证明型面板；可见文案里不再使用“证据”，只在弱来源提示中使用“留痕”。顺手将 `FlowMePage.vue` 里旧的“没有明确证据”改为“没有明确留痕”。新增 `design-qa.md`，如实标记 Product Design 截图级 QA blocked：选中的 Image Gen 源图没有本地文件路径，且本轮未暴露 Browser/Chrome 截图工具，按项目约定未使用 Playwright。验证：`rg -n "证据" frontend\src\components\workmemory\flow` 无命中、`pnpm --dir frontend build`、`git diff --check`、`wails3 task windows:package` 均通过；Vite/Rolldown 仍有第三方 `@vueuse/core` pure annotation 警告，不影响产物。新 `bin\ariadne.exe` 为 `32953856` bytes，时间 `2026-06-18 22:17:35`，SHA256 `6594F304973E47C68A25F719AA1AA0DC50F45F51BA34257CF619CB6909B851FE`；新 release zip 为 `17277763` bytes，时间 `2026-06-18 22:17:36`，SHA256 `84596E74FA0854826B85F62BF5F7FD7486B164F0957997BCB72AF13038B259E2`。 |
| 心流待办自动刷新 | completed | 针对 2026-06-18 用户反馈“待办添加成功之后，进待办页面还是看不到，要点刷新才会出现”：根因是 Agent/CLI 路径保存待办后，后端 service 已同步 SQLite，但前端 `memory.askFlow()` 只更新聊天消息，没有重新拉取 `todoList`；待办页也只读取 store 缓存。修复：`frontend/src/stores/workMemory.ts` 新增 `refreshTodos()`，`askFlow()` 完成后静默刷新待办；`frontend/src/components/workmemory/flow/useWorkMemoryFlow.ts` 在切换到 `todos` 页时静默刷新；待办页底部刷新按钮改为调用 `memory.refreshTodos()`，避免整页 `load()`。验证：`pnpm --dir frontend build`、`wails3 task windows:package`、`git diff --check` 均通过；Vite/Rolldown 仍有第三方 `@vueuse/core` pure annotation 警告，不影响产物。新 `bin\ariadne.exe` 为 `33287168` bytes，时间 `2026-06-18 21:11:53`，SHA256 `1A6A90E0EFF0B2A0474C0626A9E5D63560B079A9F1265C245567FBB6FB89793A`；新 release zip 为 `17313934` bytes，时间 `2026-06-18 21:11:54`，SHA256 `4DEE0F02C10C8BC233746F10678094D9517715514EA5887A8F6684AB6939DE6D`。 |
| 心流对话上下文传递 | completed | 针对 2026-06-18 用户反馈“上下文是不是有点问题，agents sdk 应该会自动处理上下文吗”：确认 Ariadne 的多轮 Flow 会话由本地 `work_memory_flow_messages` 持久化，OpenAI Agents SDK 只处理单次 run 内的工具循环，不会自动读取 Ariadne 历史消息。后端新增 `FlowConversationContextMessage`，`AskFlowConversation` 在每次提问前读取同一会话最近 10 条 user/assistant 消息并传入 `FlowAgentJob.Conversation`；`flowAgentPrompt` 增加 `Conversation Context` 区块和规则，只用于解析“刚才/再加一次/上面那个/继续”等指代，不替代 Work Memory/Todo 工具查询，也不能把上一轮“已保存”模型说法当作事实。GLM OpenAI-compatible Chat Tools 路径扩展 `_todo_tool_requirement`：当前问题为“你刚才没加成功，再加一次”且最近上下文包含“保存待办/待办”时，首轮强制 `add_flow_todo`。文档 `docs/plans/2026-06-18-flow-todos-and-skill.md` 补充 Conversation Context 决策和 false-save 边界。验证：`python -m py_compile internal\aiclient\flow_agents_sdk_bridge.py`、`go test ./internal/workmemory -run "ConversationPassesRecentMessages|ConversationPersists|AskFlowAgentReceivesSelfModel|AskFlowSyncsTodoWrites" -v -count=1`、`go test ./internal/aiclient -run "OpenAICompatibleFlowAgentPostsChatCompletion|FlowAgentsSDKBridgeRejectsTodoSaveWithoutTool|FlowAgentsSDKBridgeNativeSkillRouting|FlowAgentsSDKBridgePayloadEnablesNativeSkills|LooksLikeUnexecuted" -v -count=1`、`go test . ./cmd/... ./internal/...`、`wails3 generate bindings -clean=false`、`pnpm --dir frontend build`、`wails3 task windows:package`、`.\bin\ariadne.exe workmemory status` 均通过；bindings 为 `470 Packages, 23 Services, 214 Methods, 5 Enums, 201 Models, 0 Events`；Vite/Rolldown 仍有第三方 `@vueuse/core` pure annotation 警告，不影响产物。新 `bin\ariadne.exe` 为 `33287168` bytes，时间 `2026-06-18 21:01:53`，SHA256 `81F8C59CD908F383E9F939A3D2ED0F3887724EC1B589A3C316F8A726A3DAA8E8`；新 release zip 为 `17313745` bytes，时间 `2026-06-18 21:01:55`，SHA256 `FB458181A5995B503588A8E610DEF95BE1C818E1291574166A260A01BB31A7D6`。 |
| 心流待办模块与 Todo Skill | completed | 新增产品/技术依据 `docs/plans/2026-06-18-flow-todos-and-skill.md`，明确待办模型、SQLite 表、CLI/Agent skill 边界、前端独立页面和聊天归因限制；`CONTEXT.md` 补充 `Todo Item` 领域词。后端新增 `internal/workmemory/todos.go`，将 `TodoItem` 作为一等模型，使用 `work_memory_todos` / `work_memory_todo_evidence` 持久化，Wails Service 暴露 `Todos / UpsertTodo / UpdateTodo / DeleteTodo`，CLI 增加 `workmemory todos / todo-add / todo-update / todo-delete`。Agent skill 增加 `list_flow_todos / add_flow_todo / update_flow_todo`，原生 shell skill 白名单和 OpenAI-compatible Chat Tools 均接入 todo CLI，并保留“归因不明不自动落待办”的规则；针对 20:25 截图反馈“说保存了但待办为空”，补上兼容 Chat Tools 强制 `add_flow_todo`、无成功工具结果则拒绝“已保存”回答，以及 Agent 子进程 CLI 写 SQLite 后主进程同步待办的回归测试。前端新增独立 `frontend/src/components/workmemory/flow/pages/FlowTodosPage.vue`、侧栏“待办”入口、底部命令、store/API/types/bindings/CSS；浏览器烟测发现新页继承 `overflow: hidden` 会导致下方不可见，已补 `.flow-todos-page { overflow: auto }` 覆盖并验证 `overflowY=auto`、新增待办可保存并显示。验证：`python -m py_compile internal\aiclient\flow_agents_sdk_bridge.py`、`go test ./internal/workmemory -run "Todos|SelfModel|AskFlowAgentReceivesSelfModel" -v`、`go test ./internal/workmemorycli -v`、`go test ./internal/aiclient -run "FlowAgent|Unexecuted|Bridge" -v`、`go test . ./cmd/... ./internal/...`、`wails3 generate bindings -clean=false`、`pnpm --dir frontend build`、内置 Browser 对 `http://localhost:5174/` 打开待办页/滚动/新增待办烟测、`wails3 task windows:package` 均通过；bindings 为 `470 Packages, 23 Services, 214 Methods, 5 Enums, 200 Models, 0 Events`；Vite/Rolldown 仍有第三方 `@vueuse/core` pure annotation 警告，不影响产物。新 `bin\ariadne.exe` 为 `33278976` bytes，时间 `2026-06-18 20:38:52`，SHA256 `E7B13DD827592014C733FB961ABADAB34CF301AC3CD4A6698681FFC46A6DEFCC`；新 release zip 为 `17303822` bytes，时间 `2026-06-18 20:38:53`，SHA256 `31F62B7A6D2B65F8E4BE7C22F97E44F2CEDE7EE8BB20BE15C3AA501AE1762812`。 |
| 心流 GLM Chat Tools 超时修复 | completed | 针对 2026-06-18 用户截图反馈两次“Agent 调用失败”：检查 `%APPDATA%\Ariadne\ariadne.sqlite` 中 17:07:57 与 17:17:03 两条 assistant message 的 `result_json.message`，实际错误均为 `Agent runner 调用失败：OpenAI Agents SDK 执行超时`；确认当前运行进程是 `P:\workspace\glwlg\app\Ariadne\bin\ariadne.exe`，启动时间 `2026-06-18 17:07:47`，配置为 `openai-compatible + http://10.64.251.169:4000/v1 + glm-5.1`，不是用户配置缺失。直接请求 LiteLLM `/v1/chat/completions` 复现到 GLM 的工具调用兼容问题：`tool_calls[].function.arguments` 有时返回 `<tool_call>...</tool_call>` XML 而非 OpenAI 标准 JSON；手动两轮 Chat Completions（assistant tool_calls + tool result）能得到最终回答，说明后端和工具本身可用，卡点在 Agents SDK Runner 对 GLM 非标准工具参数/循环的兼容。修复：`flow_agents_sdk_bridge.py` 为 `openai-compatible` 改走 Ariadne 受控 Chat Completions tool loop，显式提供 `search_flow_memory / recent_flow_memory / get_flow_memory_entry` schema，解析 GLM XML arguments 到 JSON，执行本地 `ariadne.exe workmemory ...` 后回填 `role=tool` 消息并继续生成最终回答；OpenAI 官方路径仍保留 Agents SDK / Responses Skill；前端新增 `agent:openai-compatible-chat-tools` 标签为 `SDK 兼容工具`。真实复现验证使用同一 GLM endpoint、同一本地 workmemory CLI 与问题“我今天干了些什么？”返回 `ok=true`、`mode=agent:openai-compatible-chat-tools`，耗时约 15 秒。验证：`python -m py_compile internal\aiclient\flow_agents_sdk_bridge.py`、`go test ./internal/aiclient -run "FlowAgent|Unexecuted" -v`、`go test . ./cmd/... ./internal/...`、`pnpm --dir frontend build`、`wails3 task windows:package` 均通过；bindings 为 `470 Packages, 23 Services, 210 Methods, 5 Enums, 195 Models, 0 Events`；Vite/Rolldown 仍有第三方 `@vueuse/core` pure annotation 警告，不影响产物。新 `bin\ariadne.exe` 为 `33183744` bytes，时间 `2026-06-18 17:29:12`，SHA256 `7F467110B5DFB80CD9C814535EE08F2EDA88E194AF65CE9CC9F2F495D19E316A`；新 release zip 为 `17272179` bytes，时间 `2026-06-18 17:29:13`，SHA256 `A480D6456EFED106876BE0DF466DD5798FEB927EC80ACE3F08BD13EF0AED1CBA`。 |
| 心流 LiteLLM Chat Tools 路由 | completed | 针对 2026-06-18 用户用 LiteLLM `glm-5.1` 测试发现 `/v1/responses` 只返回未执行的 XML/function_call，而 OpenAI Agents SDK 文档建议非 OpenAI/混合提供商可走 `OpenAIChatCompletionsModel` 或 LiteLLM 适配并验证实际后端能力：调整 `flow_agents_sdk_bridge.py` 路由，`provider=openai-compatible` 即使 `nativeSkills=true` 也默认直接使用 Agents SDK `OpenAIChatCompletionsModel + function_tool` 调用 Ariadne workmemory CLI，不再先尝试 Responses ShellTool；只有 OpenAI 官方路径、`ARIADNE_FLOW_AGENT_NATIVE_SKILLS_STRICT=1` 或显式 `ARIADNE_FLOW_AGENT_ALLOW_COMPAT_NATIVE_SKILLS=1` 才尝试原生 Responses ShellTool。返回 mode 从 `agent:openai-agents-sdk-function-tool-fallback` 改为 `agent:openai-agents-sdk-chat-tools`，前端将新旧兼容 mode 均显示为 `SDK 兼容工具`，设置中心和心流设置页文案改为区分 OpenAI 官方路径与 LiteLLM 等兼容接口，避免“降级”误导。新增 `TestFlowAgentsSDKBridgeNativeSkillRouting`，并更新 fake bridge mode 断言。验证：`python -m py_compile internal\aiclient\flow_agents_sdk_bridge.py`、`go test ./internal/aiclient -run "FlowAgent|Unexecuted" -v`、`go test . ./cmd/... ./internal/...`、`pnpm --dir frontend build`、`wails3 task windows:package` 均通过；bindings 为 `470 Packages, 23 Services, 210 Methods, 5 Enums, 195 Models, 0 Events`；Vite/Rolldown 仍有第三方 `@vueuse/core` pure annotation 警告，不影响产物。新 `bin\ariadne.exe` 为 `33172480` bytes，时间 `2026-06-18 16:43:56`，SHA256 `D753F626A996B6FED9D34BD5679FA895854155F231E706581D9ECDD4D64D3C87`；新 release zip 为 `17271402` bytes，时间 `2026-06-18 16:43:57`，SHA256 `466E08EA2713E942816DBDF4FF034D67B8530E9610D1209DFB8E41C9BE46B329`。 |
| 心流我模型与聊天发言归因防错 | completed | 针对 2026-06-18 用户截图反馈“心流把别人说的话按在我身上”，并确认“心流最终目标是成为第二个我，需要一个‘我’模块记录姓名、年龄等个人信息，刚才讨论的东西都要落到文档”：新增产品/技术依据 `docs/plans/2026-06-18-flow-self-model-and-chat-attribution.md`、ADR `docs/adr/0001-flow-self-model-and-speaker-attribution.md`，并在 `CONTEXT.md` 补充 Self Model、Self Assertion、Speaker Attribution 领域词。后端新增 `internal/workmemory/self_model.go`，用 Self Assertion 存储身份、偏好、关系、边界，带确认状态和隐私权限；SQLite 增加 `work_memory_self_assertions` 与 evidence 表，Work Memory API 暴露读取、写入、删除，Flow Agent job 增加安全 Self Model 摘要，只把低风险 confirmed/allowed 条目放入默认 prompt。收紧 OCR 总结、Flow Agent、Codex Flow Agent 和 CLI guidance：聊天原文里的“你/我”属于消息发言人视角，不能自动换成当前用户；发言人或指代对象不清楚时写“群聊中有人提到”或“指代不明”。前端新增独立页面 `frontend/src/components/workmemory/flow/pages/FlowMePage.vue` 和侧栏“我”入口，可维护资料条目、权限、状态、适用范围，并展示安全摘要和归因边界；补充 types/store/API fallback/bindings/CSS。顺手把一处旧心流空状态文案里的“真正要...”句式改成产品状态文案。验证：`go test ./internal/workmemory -run "SelfModel|AskFlowAgentReceivesSelfModel|AskFlowUsesFlowAgent|AskFlowContact" -v`、`go test ./internal/aiclient ./internal/workmemorycli -run "FlowAgent|OCR|RunSearches|Unexecuted" -v`、`go test ./internal/workmemory`、`go test . ./cmd/... ./internal/...`、`pnpm --dir frontend build`、`wails3 generate bindings -clean=false`、`wails3 task windows:package` 均通过；bindings 为 `470 Packages, 23 Services, 209 Methods, 5 Enums, 195 Models, 0 Events`；Vite/Rolldown 仍有第三方 `@vueuse/core` pure annotation 警告，不影响产物。使用内置 Browser 对 `http://localhost:5174/` 做只读烟测，桌面宽度下“我”入口和页面正常渲染，390px 窄宽度下新增页可打开但 Ariadne 整体应用壳仍是桌面优先，左侧导航会压缩工作区，未把全局响应式重构并入本项。新 `bin\ariadne.exe` 为 `33166336` bytes，时间 `2026-06-18 14:49:02`，SHA256 `E21AD779AB6AEA90ACECC961D582AFBC842FED0A94F36E7E3FDF04A78D9C7092`；新 release zip 为 `17268635` bytes，时间 `2026-06-18 14:49:03`，SHA256 `754CD04ABEEE696ACE80CBBD0B0E6F7DB2D8B024D39FA1172C5A158109E9C554`。 |
| 心流首页对话区压缩与 Markdown 渲染 | completed | 针对 2026-06-17 用户反馈“上半部分占据太大空间，统计卡片没必要放首页，最上面信息可以去掉，对话框要支持 markdown 渲染”，以及后续截图反馈“贴这顶太丑，用 product-design 再设计一下”：按 Product Design brief 将首页改为对话优先但不顶贴。心流首页 `activeFlowPage === 'flow'` 时隐藏外层顶部状态/动作栏，删除首页内的标题、上下文/模型状态、后台质检/OCR/清理统计卡、自主产物卡和近期健康 chip；空会话状态单独设计为画布中段的 centered starter card，只有欢迎消息时居中显示，用户开始提问后自动切回正常从上到下的消息流，composer 固定在底部。消息气泡从纯文本 `<p>` 改为安全 Markdown 渲染，支持 `#` 标题、无序/有序列表、引用、代码块、行内代码、链接和 `**加粗**`，渲染前先转义原始 HTML，避免模型返回 HTML 被执行。相关样式新增 `.flow-stage.is-chat-home`、`.flow-chat-thread.is-empty`、`.flow-message.is-system` 和 `.flow-message-markdown` 排版。验证：Product Design context preflight、`pnpm --dir frontend build`、`wails3 task windows:package` 均通过；bindings 为 `469 Packages, 23 Services, 201 Methods, 5 Enums, 187 Models, 0 Events`；Vite/Rolldown 仍有第三方 `@vueuse/core` pure annotation 警告和大 chunk 提示，不影响产物。新 `bin\ariadne.exe` 为 `32574464` bytes，时间 `2026-06-17 11:29:24`，SHA256 `204E3396FF3E5942EF541704BA237159D9A77A4679198571FAF19F7E6BCA0D83`；新 release zip 为 `17011986` bytes，时间 `2026-06-17 11:29:25`，SHA256 `D363FF4EAA374F45E4A98A6583722CAAE9B308492FF9CE3D43F9F009A73E23EF`；本轮只走后台命令行，未操作鼠标、窗口、热键、浏览器或桌面画面，因此不声明真实桌面视觉复验。 |
| 心流 Agent tool_call 泄漏防护 | completed | 针对 2026-06-17 用户截图中 OpenAI SDK Skill 回答直接显示 `<tool_call>shell<arg_key>command</arg_key>...SKILL.md...</tool_call>`：确认不是 UI 渲染问题，而是当前 Responses-compatible 接口把模型生成的工具调用标记当普通文本返回，Agents SDK 没有真正执行 ShellTool，Ariadne 又把 `final_output` 当作最终回答展示。本轮在 Python bridge 中新增未执行 tool-call 文本识别：原生 Responses ShellTool 返回 `<tool_call>` / `<arg_key>` / JSON `tool_calls` 时不再视为成功，而是触发已有 function-tool 降级；function-tool fallback 仍返回类似协议文本时直接失败，不把内部工具协议给用户。Go runner 同步增加兜底识别，OpenAI Agents SDK 和直连兼容 runner 都会拒绝未执行工具调用文本。新增 `TestLooksLikeUnexecutedAgentToolCall`，并用 Python helper smoke 覆盖截图中的 XML 形态。验证：`python -m py_compile internal\aiclient\flow_agents_sdk_bridge.py`、bridge helper smoke、`go test ./internal/aiclient -run "Unexecuted|FlowAgent" -v`、清理 AI/Embedding/OCR 环境变量后的 `go test . ./cmd/... ./internal/...`、打包后 `ariadne.exe workmemory status`、`wails3 task windows:package` 均通过；bindings 为 `469 Packages, 23 Services, 201 Methods, 5 Enums, 187 Models, 0 Events`；Vite/Rolldown 仍有第三方 `@vueuse/core` pure annotation 警告和大 chunk 提示，不影响产物。新 `bin\ariadne.exe` 为 `32573440` bytes，时间 `2026-06-17 10:35:20`，SHA256 `C08D8C0EFE34B60814915F81EA6282D917DCC43E3E8794D936753B82EA3DE047`；新 release zip 为 `17011440` bytes，时间 `2026-06-17 10:35:21`，SHA256 `AF8D40F7ED4950A41799D6CB8CEB5A8364C252C72A863D8A82616F65430B18E2`；本轮只走后台命令行，未操作鼠标、窗口、热键或桌面画面，因此不声明真实桌面 Flow Agent 提问复验。 |
| 心流 Agent Responses 原生 Skill 兼容 | completed | 针对 2026-06-17 用户反馈“AI 接口支持 responses 接口，可以改掉 openai-compatible 对 OpenAI Agent SDK 支持不够好的遗留问题”：确认原 `flow_agents_sdk_bridge.py` 只有 OpenAI 官方/Azure 或显式 `nativeSkills` 才走 `OpenAIResponsesModel + ShellTool`，`provider=openai-compatible` 默认降级到 SDK `function_tool`。本轮新增 settings schema v14 的 `agentResponsesEnabled` 开关并默认开启，Flow agent policy/job 增加 `nativeSkills`，`openai-compatible` 在该开关打开时会先尝试 OpenAI Agents SDK native Responses shell Skill；如果当前兼容接口实际不支持 `/responses`，bridge 会保留原 function-tool fallback，只有设置严格 native env 时才直接失败。设置中心和心流模型设置页新增 `Responses 原生 Skill` 开关，文案说明支持 `/responses` 时优先原生 ShellTool，失败回退工具降级。新增/更新 `TestFlowAgentsSDKBridgePayloadEnablesNativeSkills`、settings v13->v14 migration 测试和 AskFlow runner policy 测试。验证：`go test ./internal/settings ./internal/aiclient ./internal/workmemory -run "AgentResponses|FlowAgent|AskFlow|Settings|Sensitive" -v`、清理 AI/Embedding/OCR 环境变量后的 `go test . ./cmd/... ./internal/...`、`wails3 generate bindings -clean=false`、`pnpm --dir frontend build`、`wails3 task windows:package` 均通过；bindings 为 `469 Packages, 23 Services, 201 Methods, 5 Enums, 187 Models, 0 Events`；Vite/Rolldown 仍有第三方 `@vueuse/core` pure annotation 警告和大 chunk 提示，不影响产物。新 `bin\ariadne.exe` 为 `32570880` bytes，时间 `2026-06-17 10:18:00`，SHA256 `A2DFF4DF49A6F98422FA7B007C45F438C6D57958582397BE58B8291E9E15DD4C`；新 release zip 为 `17008416` bytes，时间 `2026-06-17 10:18:01`，SHA256 `2D2D40B0D0BBE92D0D3E226EBBDB812190BC9E0B4164E9E5C3BE5DA7B3ACE7F3`；本轮只走后台命令行，未操作鼠标、窗口、热键或桌面画面，因此不声明真实桌面 Flow Agent 提问复验。 |
| 心流敏感判断降误伤 | completed | 针对 2026-06-17 用户反馈“现在对敏感的判断太敏感，正常截图也判断为敏感”：确认旧 `looksSensitive` 同时存在于 `workmemory` 与 `ocr`，直接用 `strings.Contains` 匹配 `password`、`token`、`secret`、`验证码`、`密码` 等词，导致登录页说明、配置说明、验证码按钮等正常截图被隔离。现改为共享的凭据形态检测：只有 `password=...`、`Authorization: Bearer ...`、Cookie/Set-Cookie、私钥块、SSH key、JWT/OpenAI/GitHub token 形态、中文 `密码/密钥/令牌/验证码: 值` 等真实凭据样式才自动敏感；普通 `密码输入框`、`token 配置说明`、`secret/API key 概念`、`验证码按钮` 不再自动敏感。`ocr.Service` 复用 `workmemory.LooksSensitiveText`，`enrichEntry` 不再因为旧标签/正文关键词二次自动置敏，图片材料导入不再靠文件名/tag 误判；`sensitiveRulesEnabled` 设置已通过 `main.applyWorkMemoryRuntime` 进入 `CapturePolicy`，关闭自动敏感规则后仍保留手动 `Sensitive=true`。新增/更新 `TestSensitiveDetectionRequiresCredentialShape`、`TestSensitiveRulesDoNotFlagPlainKeywordScreenshots`、`TestSensitiveRulesCanBeDisabledForAutomaticClassification`、`TestApplyOCRTextDoesNotMarkPlainSensitiveWords`、`TestRecognizeImageSensitiveFlagRequiresCredentialShape`。验证：`go test ./internal/workmemory ./internal/ocr -run "Sensitive|OCR|Quality|ReviewPending" -v`、清理 AI/Embedding/OCR 环境变量后的 `go test . ./cmd/... ./internal/...`、`pnpm --dir frontend build`、`wails3 generate bindings -clean=false`、`wails3 task windows:package` 均通过；bindings 为 `469 Packages, 23 Services, 201 Methods, 5 Enums, 187 Models, 0 Events`；Vite/Rolldown 仍有第三方 `@vueuse/core` pure annotation 警告和大 chunk 提示，不影响产物。新 `bin\ariadne.exe` 为 `32568320` bytes，时间 `2026-06-17 10:10:36`，SHA256 `62E35EC8E05750377B034987E3BB2B23B9AE3EDB0D2CB1E3BF094E38AB4740D4`；新 release zip 为 `17007560` bytes，时间 `2026-06-17 10:10:37`，SHA256 `92E0D1EA69427AAEAFEC6BC11F2A15C52F650C13720CF5A975D3532BC815426F`；本轮只走后台命令行，未操作鼠标、窗口、热键或桌面画面，因此不声明真实桌面敏感截图 UI 复验。 |
| 心流时间线 OCR 补跑/重跑勾选项 | completed | 针对 2026-06-17 用户要求“补跑操作改成补跑/重跑我勾选的，不要自动选择”：确认原 `timelineBatchOcrEntries` 在没有勾选时会自动回退到当前筛选中的 `needsOCRSummary` 候选，导致补跑按钮隐式处理未勾选记录。现改为只处理 `timelineSelectedEntries.filter(canRunTimelineOCR)`；没勾选、勾选中无可处理截图或批处理中都会禁用，不再自动挑当前结果。按钮文案改为 `补跑/重跑我勾选的` / `补跑/重跑已勾选 N` / `无可处理勾选项`，运行态文案为 `正在补跑/重跑勾选项`，tooltip 说明非图片、敏感和待质检记录会跳过；“选择当前结果”保留为用户显式批量勾选入口。验证：`pnpm --dir frontend build`、`wails3 task windows:package` 均通过；bindings 为 `469 Packages, 23 Services, 201 Methods, 5 Enums, 187 Models, 0 Events`；Vite/Rolldown 仍有第三方 `@vueuse/core` pure annotation 警告和大 chunk 提示，不影响产物。新 `bin\ariadne.exe` 为 `32563712` bytes，时间 `2026-06-17 09:38:37`，SHA256 `C2123BCBCC790AF35EC3627FC901E37DD4D64905B8C7BCB4F81C750FEC8B3B85`；新 release zip 为 `17003755` bytes，时间 `2026-06-17 09:38:40`，SHA256 `8F3766E3D4776B406F21EBBBABA0490289871B6FBA099F241576D63993510079`；本轮只走后台命令行，未操作鼠标、窗口、热键或桌面画面，因此不声明真实桌面时间线点击复验。 |
| 心流质检触发及时化 | completed | 针对 2026-06-16 用户反馈“感觉一直没触发质检”：检查本机 `%APPDATA%\Ariadne\work_memory.json` 后确认 13 条工作记忆全部仍是 `qualityStatus=pending`，`checked=0`，`lastQualityCheckAt=null`。根因是 worker 只在一小时 `qualityTicker` 上调用 `ReviewPendingCaptures()`，同时当前活跃窗口 session 会被跳过，导致刚采集后或一直停在同一窗口时长期显示待质检。本轮改为 worker 启动时异步补跑一次质检，并在前台窗口签名变化时异步质检上一段已结束 pending session；新 active session 仍受 `currentWindowSessionEntryID` 保护，不会提前进入总结/检索。异步质检新增 `qualityReviewRunning` 串行保护，避免快速切窗口并发触发 OCR；重复/近似时间机器画面合并后重新置为 pending，避免已质检旧记录合并新画面后漏检。新增 `TestWindowChangeTriggersReviewForEndedPendingSession`。验证：`go test ./internal/workmemory -run "WindowChangeTriggersReview|ReviewPending|Quality|TimeMachineWindowSession|AutoOCR" -v`、`go test ./internal/workmemory ./internal/ocr ./internal/aiclient -v`、清理 AI/Embedding/OCR 环境变量后的 `go test . ./cmd/... ./internal/...`、`pnpm --dir frontend build`、`wails3 task windows:package` 均通过；bindings 为 `469 Packages, 23 Services, 201 Methods, 5 Enums, 187 Models, 0 Events`；Vite/Rolldown 仍有第三方 `@vueuse/core` pure annotation 警告和大 chunk 提示，不影响产物。新 `bin\ariadne.exe` 为 `32563712` bytes，时间 `2026-06-16 22:56:55`，SHA256 `7AEB737B4A926D301D9F3555CC21C50DA9A22BD36E4D1EFDB38468B3CDE4D44C`；新 release zip 为 `17003682` bytes，时间 `2026-06-16 22:56:56`，SHA256 `155605CD58039C31EE4CE246FC3D5E899232F6C6331093F142EC7AC8CF83AF39`；本轮只走后台命令行和本地 JSON 检查，未操作鼠标、窗口、热键或桌面画面，因此不声明真实桌面质检 UI 复验。 |
| GLM-OCR Ollama generate OCR 兼容 | completed | 针对 2026-06-16 用户已在另一台电脑用 Ollama 部署 `glm-ocr:latest` 并调通 `http://192.168.1.11:11434/api/generate`，确认旧实现只支持 OpenAI-compatible `/chat/completions`，不兼容 Ollama 原生视觉请求。本轮新增 OCR provider `ollama-generate` / `ollama_generate` / `ollama`，读取图片文件后把裸 base64 放入 JSON `images` 数组发送到 Ollama `/api/generate`；不使用 multipart 文件上传，也不使用 OpenAI `image_url` data URL。`ocrBaseUrl` 可填完整 `/api/generate` 端点、Ollama 根地址或 `/api`，代码会归一化；该 provider 不要求 API key，OpenAI-compatible OCR 路径保持不变。心流设置页和设置中心文案/placeholder 已同步提示 `openai-compatible / ollama-generate`、`glm-ocr:latest` 和示例地址。验证：`go test ./internal/aiclient ./internal/ocr ./internal/settings -run "OCR|Ollama|ApplyOCRAIRuntime|Settings" -v`、清理 AI/Embedding/OCR 环境变量后的 `go test . ./cmd/... ./internal/...`、`pnpm --dir frontend build`、使用 `assets/logo.png` 对 `http://192.168.1.11:11434/api/generate` 做真实 base64 `images` smoke 并返回 `done=true`、`wails3 task windows:package` 均通过；bindings 为 `469 Packages, 23 Services, 201 Methods, 5 Enums, 187 Models, 0 Events`；Vite/Rolldown 仍有第三方 `@vueuse/core` pure annotation 警告和大 chunk 提示，不影响产物。新 `bin\ariadne.exe` 为 `32561664` bytes，时间 `2026-06-16 22:33:45`，SHA256 `660C37415DA53DE7E7AC212F8E480B2325277FBC9B7FCA87304C84DA905E1173`；新 release zip 为 `17002209` bytes，时间 `2026-06-16 22:33:46`，SHA256 `D61567BCE2C179DA762A2757567E8D1B0C026BE808EEB3FF34EBE74ABC55B9CD`；本轮只走后台命令行，未操作鼠标、窗口、热键或桌面画面，因此不声明真实桌面 OCR 点击复验。 |
| 心流待质检截图 OCR 预检前置 | completed | 针对 2026-06-16 用户指出“心流需要先质检再做其他操作，但 OCR 不做会影响质检效果”：`workmemory.Entry` 新增 `qualityOcrText` / `qualityOcrStatus`，pending 截图执行 OCR 时只写入质检 OCR 字段，不进入正式 `ocrText`、FTS、日报、复盘、经验发现、embedding/Milvus、心流问答或 Agent evidence；`ReviewPendingCaptures()` 调整为先对非活跃 pending 截图做质检 OCR 预处理，再折叠重复帧并标记 checked，checked 后将质检 OCR 提升为正式 OCR 写回和标题/摘要整理。`workmemory.Search()` 对非空查询跳过 pending，`fts.Rebuild()` 只索引非敏感且非 pending 的可抽取记录。心流首页 OCR 健康卡新增“预检/正式”区分，health summary 增加 `qualityOcrDone/qualityOcrPending/qualityOcrFailed`。验证：`go test ./internal/workmemory ./internal/ocr -run "OCR|Quality|ReviewPending" -v`、清理 AI/Embedding/OCR 环境变量后的 `go test . ./cmd/... ./internal/...`、`pnpm --dir frontend build`、`wails3 generate bindings -clean=false`、`wails3 task windows:package` 均通过；bindings 为 `469 Packages, 23 Services, 201 Methods, 5 Enums, 187 Models, 0 Events`；Vite/Rolldown 仍有第三方 `@vueuse/core` pure annotation 警告和大 chunk 提示，不影响产物。新 `bin\ariadne.exe` 为 `32547328` bytes，时间 `2026-06-16 22:23:59`，SHA256 `F6C68FFFF18430891582EC3C418DEB36D75BACC1F367CBB0A2E0C9B38A4D0DF0`；新 release zip 为 `16998396` bytes，时间 `2026-06-16 22:24:01`，SHA256 `FBABF4786B0E13954AEE19079CCAD1A4918664F63D3FCB2EA6E898EAD266F639`；本轮只走后台命令行，未操作鼠标、窗口、热键或桌面画面，因此不声明真实桌面质检 OCR 视觉复验。 |
| Ariadne 正式替换 x-tools 根目录 | completed | 将旧 Python/PyQt x-tools 源码归档到 `legacy/x-tools-python`，Ariadne Wails3/Vue3 工程从 `experiments/ariadne` 移到仓库根目录；后续命令统一在 `P:\workspace\glwlg\app\x-tools` 执行。根 `.gitignore` 已同步忽略根目录构建产物、前端依赖/产物和旧实验残留；Ariadne 生产代码中的 Python 旧插件 bridge 已删除，不再注册或执行旧 Python 插件，旧源码只作为归档参考。验证：`go test . ./cmd/... ./internal/...`、`vue-tsc --noEmit`、`pnpm --dir frontend build`、`wails3 generate bindings -clean=false`、`wails3 task windows:package` 均通过；Vite/Rolldown 仍有第三方 `@vueuse/core` pure annotation 警告和大 chunk 提示，不影响产物；bindings 为 `469 Packages, 23 Services, 201 Methods, 5 Enums, 187 Models, 0 Events`；新 `bin\ariadne.exe` 为 `32534528` bytes，时间 `2026-06-16 21:50:57`，SHA256 `957A2A490193A6DD658F020FD3963C601B0F195E463399521A730EF74DF6659A`；新 release zip 为 `16987699` bytes，时间 `2026-06-16 21:50:58`，SHA256 `CD5A45D44B23C783795E73627EB0737BA3A1777DF3F650FF73C469A6C898EC24`；本轮只走后台命令行，未操作鼠标、窗口、热键或桌面画面。 |
| 心流联系人证据边界与前台窗口采集默认 | completed | 针对 2026-06-16 用户反馈“我今天跟谁聊过天”回答人物关系对不上，并追问“截图可以做到只截当前窗口而不截全屏吗”：检查当前 `%APPDATA%\Ariadne` 时间线 JSON、FTS 状态和原始截图后确认，核心不是 OCR 完全失败，而是全屏微信截图同时包含当前会话、左侧会话列表、群预览、服务号和后台窗口，后续 OCR 整理与 agent 推理把“界面可见人名”误当成“实际聊天对象”。已收紧大模型 OCR 总结提示、Flow agent system prompt、Flow Memory Skill 内容和 `workmemory` CLI guidance：联系人类问题必须把搜索/recent 结果当候选，优先 `get` 明细，明确区分当前会话正文、侧边栏、后台窗口、服务号、群预览和用户自己的右侧消息。自动时间机器默认与迁移范围改为 `active_window`，减少全屏干扰；settings schema 升至 v13；打包后 `ariadne.exe workmemory status` 已触发本机配置迁移为 `captureScope=active_window`。验证：`go test ./internal/settings ./internal/workmemory ./internal/workmemorycli ./internal/aiclient -v`、`go test ./...`、`vue-tsc --noEmit`、`pnpm build`、`wails3 generate bindings -clean=false`、打包后 `ariadne.exe workmemory status`、`wails3 task windows:package` 均通过；bindings 为 `470 Packages, 23 Services, 200 Methods, 5 Enums, 184 Models, 0 Events`；新 `bin/ariadne.exe` 为 `32550912` bytes，时间 `2026-06-16 20:32:53`，SHA256 `B27AE147BB60B94FCAD2E5C5299C422667505676AEA3B9F39253BECD511256A0`；新 release zip 为 `16999305` bytes，时间 `2026-06-16 20:32:55`，SHA256 `7F62749556CB161E967B526EF75F6364B8D48CFE94686EFAE4988BACBDA511B3`；本轮只走后台命令行和本地截图文件检查，未操作鼠标、窗口、热键或桌面画面，因此不声明真实桌面 Flow 提问复验 |
| 心流通用 Agent 与 Flow Memory Skill/CLI | completed | 针对 2026-06-16 用户提醒“agent 应该是通用型 agent，心流数据封装为 skill 给它调用，OpenAI Agent SDK 支持 skill，不用自己实现解析”：新增 `internal/workmemorycli` 作为统一 JSON CLI，`cmd/workmemoryctl` 和打包后的 `ariadne.exe workmemory ...` 共享同一套 `status / refresh / search / recent / get / add-note` 动作；`AskFlow` 在默认 `openai-agent` 路径不再用 Go 侧硬编码分词/意图检索预选证据，而是把事实型问题交给 OpenAI Agents SDK runner，由 runner 通过 Ariadne Flow Memory 工具查询本地非敏感、已质检时间线/OCR/剪贴板/向量与关键词索引。`flow_agents_sdk_bridge.py` 优先支持 OpenAI Responses local shell Skill：生成 `ariadne-flow-memory/SKILL.md`，通过 SDK `ShellTool(environment.skills=[...])` 暴露给模型，shell executor 仅允许读取该 SKILL.md 和运行 `ariadne workmemory ...`；当当前 provider 是 OpenAI-compatible Chat Completions（例如本机 `glm-5.1` 代理）时，明确降级为 SDK `function_tool` 调用同一 CLI，不再伪装成原生 Skill。前端回答气泡新增 mode 区分 `OpenAI SDK Skill` 与 `OpenAI SDK 工具降级`。验证：bridge 解析自检、打包后 `ariadne.exe workmemory status` 返回 `work memory cli ready`、`go test ./internal/aiclient ./internal/workmemory ./internal/workmemorycli -v`、清理 AI/Embedding/OCR 环境变量后的 `go test ./...`、`vue-tsc --noEmit`、`pnpm build`、`wails3 generate bindings -clean=false`、`wails3 task windows:package` 均通过；bindings 为 `470 Packages, 23 Services, 200 Methods, 5 Enums, 184 Models, 0 Events`；新 `bin/ariadne.exe` 为 `32547328` bytes，时间 `2026-06-16 18:16:03`，SHA256 `C2D682008089549828AB4B4C2DC5417D6D93208DB8B42537A92AB4BDA4BCD9FD`；新 release zip 为 `16997656` bytes，时间 `2026-06-16 18:16:04`，SHA256 `77ED5449F24E0897C4495D4BB648E3EAC2E21EA57E4E06CFA1AA2CBFBD7510D9`；本轮只走后台命令行，未操作鼠标、窗口、热键或桌面画面，因此不声明真实桌面 Flow 提问复验 |
| 大模型 OCR 可配置优先与本地 OCR 回退 | completed | 针对 2026-06-16 用户要求“ocr 要支持配置大模型 ocr，如果没有配置就降级为现有的 ocr”：`ocr.Service` 新增 `AIOCRPolicy` 和 `AIClient`，识别图片时先判断 `ocrModelEnabled + ocrModel + provider` 是否完整，完整则通过 OpenAI-compatible 视觉模型识别图片文字；大模型 OCR 未配置、未启用、返回空内容或报错时自动回退现有 RapidOCR bridge，不中断自动 OCR、手动 OCR、批量 OCR+总结、贴图 OCR 等既有入口。`aiclient` 新增 `OpenAICompatibleImageOCR`，向 `/chat/completions` 发送 `data:image/...;base64` 多模态消息，API key 读取优先级为 `ARIADNE_OCR_API_KEY`、`ARIADNE_AI_API_KEY`、`OPENAI__API_KEY`、`OPENAI_API_KEY` 和 Credential Manager；识别结果按行转回 OCR lines。设置版本升至 12，新增 `ocrModelEnabled / ocrProvider / ocrBaseUrl / ocrModel`，心流设置“模型与向量”页可配置，文案明确“大模型优先，失败回退本地”。验证：`go test ./internal/ocr ./internal/aiclient ./internal/settings -v`、清理 AI/Embedding/OCR 环境变量后的 `go test ./...`、`vue-tsc --noEmit`、`pnpm build`、`wails3 generate bindings -clean=false`、`wails3 task windows:package` 均通过；bindings 为 `469 Packages, 23 Services, 200 Methods, 5 Enums, 184 Models, 0 Events`；新 `bin/ariadne.exe` 为 `32472064` bytes，时间 `2026-06-16 17:31:24`，SHA256 `96C6E496CFCE6490BFB1CBC9197A48DB928E84F6109278D825718EC204D87623`；新 release zip 为 `16960495` bytes，时间 `2026-06-16 17:31:26`，SHA256 `AAC943156467A8BE7C8EA6CA469FDB3EB4013E10FD5B7B9CDFB22C431DB9DD36`；本轮只走后台命令行，未操作鼠标、窗口、热键或桌面画面，因此不声明真实桌面 OCR 点击复验 |
| 心流 OpenAI Agents SDK 默认 runner 修复 | completed | 针对 2026-06-16 用户质疑“心流没有实际检索内容、默认不该走 Codex、是不是并没有通过 OpenAI Agent SDK 实现内置 agent”：确认上一轮 `openai-agent` 名称实际仍是直接 OpenAI-compatible chat completion，已修正为默认通过 Ariadne 本地检索/过滤证据后调用 Python OpenAI Agents SDK sidecar。新增 `flow_agents_sdk_bridge.py`，使用 `Agent` / `Runner` / `OpenAIChatCompletionsModel` 生成动态回答；Codex 只有 `CodexCollaborationEnabled` 或 `ARIADNE_FLOW_AGENT=codex` 显式开启才用，`ExternalAgentEnabled` 只控制任务包，不再触发普通心流问答走 Codex。联系人问题增加 `跟/聊/说了什么` 意图识别和人名 evidence 排序；runner 缺失、SDK runtime 缺失、SDK 报错或空回答时返回可见 `agent_error`，不再把本地统计摘要伪装为动态回答。前端文案改为 `OpenAI Agents SDK 问答` / `Agent sidecar`；直连兼容 runner 的 mode 改为 `agent:openai-compatible-direct`。本机已创建 `%LOCALAPPDATA%\Ariadne\agent-python` 并安装 `openai-agents 0.17.5`，导入验证通过；本地 mock server 无真实 key 烟测验证 SDK 调用链返回 `mock sdk answer`；默认跳过旧 x-tools `.venv`，除非显式设置 `ARIADNE_FLOW_AGENT_ALLOW_REPO_PYTHON=1`。验证：`go test ./internal/aiclient ./internal/workmemory -run "FlowAgent|AskFlow" -v`、清理 AI/Embedding 环境变量后的 `go test ./...`、`vue-tsc --noEmit`、`pnpm build`、`wails3 generate bindings -clean=false`、`wails3 task windows:package` 均通过；bindings 为 `469 Packages, 23 Services, 199 Methods, 5 Enums, 183 Models, 0 Events`；新 `bin/ariadne.exe` 为 `32405504` bytes，时间 `2026-06-16 17:04:58`，SHA256 `DA4CD5429493D16E12777EFF25F297957BBDCFFFB509F8B33F97E721A01AFD0A`；新 release zip 为 `16934844` bytes，时间 `2026-06-16 17:05:00`，SHA256 `901B60F908A3678856B07640881B35E33B60D477C230C2DC38A4B403F3916630`；本轮只走后台命令行，未操作鼠标、窗口、热键或桌面画面，因此不声明真实桌面 Flow 提问复验 |
| 独立工具窗口路由优先与标题栏图标补强 | completed | 针对 2026-06-16 用户反馈“网络小窗异常、设置界面显示为心流页面、左上角还是没显示品牌 logo、任务栏按钮已经正常”：确认根因是前端模板条件先判断共享 `appShell.activeView === 'work-memory'`，而 `standaloneToolView` 虽然已从 `?view=settings` / `?view=network-mini` 解析正确，却被默认心流 activeView 抢先渲染；修复后新增 `shouldRenderToolView()`，独立窗口 URL view 优先于全局 activeView，`settings` 和 `network-mini` 不再误渲染心流。普通主/工具窗口 mounted 后调用 `ApplyMainWindowPolicy()`，后端 `EnableTaskbarToggle()` 复用完整普通窗口策略，不只恢复任务栏样式，还立即设置 Ariadne small/big icon 并在 80ms/300ms/900ms 后重试，规避 Wails/WebView HWND 尚未完全就绪导致标题栏仍显示默认图标。验证：`vue-tsc --noEmit`、`go test ./internal/toolwindows ./internal/shell`、`pnpm build`、清理 AI/Embedding 环境变量后的 `go test ./...`、`wails3 task windows:package` 均通过；bindings 为 `469 Packages, 23 Services, 199 Methods, 5 Enums, 183 Models, 0 Events`；新 `bin/ariadne.exe` 为 `32335872` bytes，时间 `2026-06-16 16:22:32`，SHA256 `DD4A23DC6589ECE7648E907567B4711C749A9265334252F13B7C001E467A6320`；新 release zip 为 `16899588` bytes，时间 `2026-06-16 16:22:34`，SHA256 `69DD3721E843328308085F2EE16E799C49A414E149C47E0759EAB7584326829A`；本轮只走后台命令行，未操作鼠标、窗口、热键或桌面画面，因此不声明真实设置/网络小窗/标题栏图标视觉复验 |
| 心流主窗口化与启动器附属化 | completed | 针对 2026-06-16 用户反馈“任务栏图标还是要发起截图操作才会出现，应该把心流窗口作为主体，启动器是附带的，Ariadne 启动默认展示心流”：将 Wails `main` 窗口从无头/隐藏任务栏的 launcher 改为普通原生心流主窗口，标题 `Ariadne - 心流`，默认尺寸 `1280x820`、最小 `1040x640`、`HiddenOnTaskbar=false`、`DisableIcon=false`；`ShowLauncher()` 不再复用 `main`，而是创建/显示独立 `tool-launcher`，URL 为 `/?view=launcher`，继续透明、无头、隐藏任务栏，只承担 Alt+Q 搜索器职责；`Open("work-memory")` 统一恢复/聚焦主心流窗口，设置、历史、Hosts、工作流等仍作为独立普通工具窗口可与心流并存；二次启动和托盘单击改为回到心流，托盘菜单保留“打开启动器”。前端无 `view` 参数时渲染心流并启用普通窗口模式，`?view=launcher` 才渲染启动器；启动器执行打开工具动作后隐藏自身，不关闭心流/设置。验证：`vue-tsc --noEmit`、`go test ./internal/toolwindows ./internal/shell`、`pnpm build`、清理 AI/Embedding 环境变量后的 `go test ./...`、`wails3 task windows:package` 均通过；bindings 为 `469 Packages, 23 Services, 199 Methods, 5 Enums, 183 Models, 0 Events`；新 `bin/ariadne.exe` 为 `32334336` bytes，时间 `2026-06-16 16:11:19`，SHA256 `49675D8E80B1C5CC4AA20A0F8AA75F458914C20191E17237F99013D58EB69189`；新 release zip 为 `16898132` bytes，时间 `2026-06-16 16:11:20`，SHA256 `0F2DCA64F1A082623EB5639EB8BA3162919E03B6663C65031F446E57FB1CDD5F`；本轮只走后台命令行，未操作鼠标、窗口、热键或桌面画面，因此不声明真实任务栏点击/图标视觉复验 |
| 普通工具窗口独立化和品牌图标修复 | completed | 针对 2026-06-16 用户反馈“设置窗口正常但没有任务栏图标；心流窗口乱、截图后消失；心流也应该和设置一样是独立窗口，两个窗口可同时出现；窗口左上角缺品牌 logo”：确认根因是 `toolWindowsApi.ts` 用 `@vite-ignore` + 字符串动态 import，Vite 生产包没有把 `toolwindows/service.js` binding 打入 bundle，打开工具窗口失败后进入主窗口 fallback；主窗口 `HiddenOnTaskbar=true` 导致设置/心流没有独立任务栏图标，心流还会被截图流程的主窗口恢复逻辑覆盖。修复后 `toolwindows` binding 改为静态动态 import 并显式处理无 `.d.ts` 的生成文件；生产包中工具窗口服务失败时不再静默 fallback 到主窗口；launcher 等待独立窗口打开结果再显示反馈。`work-memory` 工具窗标题改为 `Ariadne - 心流`；普通工具窗口继续保留 native titlebar/taskbar，并通过 Windows helper 对 `WM_SETICON` 设置 Ariadne small/big icon；`toolwindows.SetWindowIcon(appIcon)` 在启动时注入品牌图标。验证：`go test ./internal/toolwindows -v`、`vue-tsc --noEmit`、`pnpm build`、清理 AI/Embedding 环境变量后的 `go test ./...`、`wails3 task windows:package` 均通过；bindings 为 `469 Packages, 23 Services, 198 Methods, 5 Enums, 183 Models, 0 Events`；新 `bin/ariadne.exe` 为 `32327168` bytes，时间 `2026-06-16 15:42:11`，SHA256 `547C25BD6A60A0F75734BB67825808DBE98DA8834B72CA5E0A294A955C9D20D2`；新 release zip 为 `16893943` bytes，时间 `2026-06-16 15:42:12`，SHA256 `D077DFA03D96E5EE3BF24F7BA6B4F1F261FF0274D64353059C43E5BEB205F5A2`；本轮只走后台命令行，未操作鼠标、窗口、热键或桌面画面，因此不声明真实任务栏图标/心流与设置并存视觉复验 |
| 普通业务窗口恢复原生窗口框架 | completed | 针对 2026-06-16 用户澄清“普通窗口不该是无头窗口，只有启动器和网络小窗这类特殊窗体才不需要头”：撤回上一轮继续补无头窗口样式的方向。普通工具窗口恢复 Wails/Windows 原生标题栏、边框拖拽、任务栏点击最小化/还原和系统窗口行为；`toolwindows.frameless(view)` 只对 `network-mini` 返回 true；前端移除普通工具窗口的自绘 `WindowControls`，避免和原生标题栏重复；主 `Ariadne` 启动器继续无头但 `HiddenOnTaskbar=true`，避免任务栏图标指向 launcher 这个特殊窗体。启动器、截图遮罩、贴图、网速小窗仍保持特殊无头策略，其中网速小窗继续隐藏任务栏。验证：`go test ./internal/toolwindows -v`、`vue-tsc --noEmit`、清理 AI/Embedding 环境变量后的 `go test ./...`、`pnpm build`、`wails3 task windows:package` 均通过；bindings 为 `469 Packages, 23 Services, 197 Methods, 5 Enums, 183 Models, 0 Events`；新 `bin/ariadne.exe` 为 `32321024` bytes，时间 `2026-06-16 15:18:26`，SHA256 `796DA15631D11E3E068FE08A43BEBF3EDCEFD8E1FCD85D0E506B5691A906D82A`；新 release zip 为 `16888516` bytes，时间 `2026-06-16 15:18:27`，SHA256 `8156419DC2C0BF628B3A0C2009E245FA516E44442C32CEDD56A9B63BC668F2FF`；本轮只走后台命令行，未操作鼠标、窗口、热键或桌面画面，因此不声明真实任务栏点击复验 |
| 普通工具窗口任务栏点击最小化修复 | completed | 针对 2026-06-16 用户反馈“点任务栏的图标不能最小化 Ariadne，其他程序都可以”：确认第一次修复只在 Wails 运行时 `SetFrameless(true)` 生成的 `WS_POPUP` 窗口上补 `WS_SYSMENU/WS_MINIMIZEBOX` 等标志，不足以让 Explorer 任务栏按普通窗口 toggle。当前修复改为普通工具窗创建阶段即使用 Wails frameless，由 Wails `WM_NCCALCSIZE` 隐藏原生标题栏；前端普通工具窗不再重复调用运行时 `SetFrameless(true)`；`toolwindows.EnableTaskbarToggle(ctx, view)` 会恢复 `WS_OVERLAPPEDWINDOW | WS_VISIBLE`、清除 `WS_POPUP`，确保 `WS_EX_APPWINDOW`，清除 `WS_EX_TOOLWINDOW/WS_EX_NOACTIVATE/WS_EX_TOPMOST`，清空 owner 并移出 topmost z-order。启动器、截图遮罩、贴图、网速小窗不应用该样式。验证：`go test ./internal/toolwindows -v`、`vue-tsc --noEmit`、清理 AI/Embedding 环境变量后的 `go test ./...`、`pnpm build`、`wails3 task windows:package` 均通过；bindings 为 `469 Packages, 23 Services, 197 Methods, 5 Enums, 183 Models, 0 Events`；新 `bin/ariadne.exe` 为 `32325120` bytes，时间 `2026-06-16 15:07:24`，SHA256 `53721642A1A6603D0956F3AAD21DF4ADF078C5F9E2D67D7DB684F9D0128C427F`；新 release zip 为 `16893366` bytes，时间 `2026-06-16 15:07:26`，SHA256 `F40E0595E0F928405D205947F9E16FFCFB607C5DE3DD7F8B26FB01A864EBCEDD`；本轮只走后台命令行，未操作鼠标、窗口、热键或桌面画面，因此不声明真实任务栏点击复验 |
| 心流时间线程序筛选可搜索下拉框 | completed | 针对 2026-06-16 用户要求“时间线的按程序筛选改成下拉框选择，下拉框要支持搜索”：`WorkMemoryCenter.vue` 将时间线来源程序横向 chip 列表改为自定义 combobox，下拉中保留“全部程序”和各程序计数，支持按程序展示名或内部 id 搜索，打开后自动聚焦搜索框，选择后复用原 `setTimelineAppFilter` 逻辑并重置分页/批量选择；点击外部或 Esc 关闭。`style.css` 新增下拉触发器、搜索框、选项列表、空状态和选中态样式，保持心流当前视觉系统。验证：`vue-tsc --noEmit`、`pnpm build`、`wails3 task windows:package` 均通过；Vite/Rolldown 仍有第三方 `@vueuse/core` pure annotation 警告和大 chunk 提示，不影响产物；bindings 为 `469 Packages, 23 Services, 196 Methods, 5 Enums, 183 Models, 0 Events`；新 `bin/ariadne.exe` 为 `32318464` bytes，时间 `2026-06-16 14:35:15`，SHA256 `94AAB8E7B6ACFF161759D285945E69E23115738F686D1463C848147944C02986`；新 release zip 为 `16884506` bytes，时间 `2026-06-16 14:35:16`，SHA256 `CD24BB2DAE27039F02D7194124BE23530B84622CE4C816B9AC645B6DB071EE3A`；本轮只走后台命令行，未操作鼠标、窗口、热键或桌面画面，因此不声明真实桌面下拉筛选点击复验 |
| 心流时间线批量 OCR+总结 | completed | 针对 2026-06-16 用户要求“已有数据很多没有 OCR，增加批量 OCR 按钮，注意是 OCR+总结并加进度条”：心流时间线新增 `批量 OCR+总结` 按钮；已勾选轨迹时处理勾选项，未勾选时处理当前筛选下有图片证据且缺 OCR 或标题/摘要仍为泛化截图记录的条目。批量逻辑逐条调用现有 `RecognizeWorkMemory -> ApplyOCRText` 链路，因此会执行 OCR 写回、本地标题/摘要/正文整理，以及启用 AI 时的 OCR 总结；敏感记录和待质检记录默认跳过。新增独立 OCR 进度状态和进度条，显示 `done/total` 与当前处理对象，删除进度互不干扰。验证：`vue-tsc --noEmit`、`pnpm build`、`go test ./internal/ocr ./internal/workmemory -run "OCR|ApplyOCR|RecognizeWorkMemory" -v`、`wails3 task windows:package` 均通过；Vite/Rolldown 仍有第三方 `@vueuse/core` pure annotation 警告和大 chunk 提示，不影响产物；bindings 为 `469 Packages, 23 Services, 196 Methods, 5 Enums, 183 Models, 0 Events`；新 `bin/ariadne.exe` 为 `32315392` bytes，时间 `2026-06-16 14:30:13`，SHA256 `9D04ACE8F7CFEC9D70F4A021919753823B2F1B5220398D95B30771F1294FA03F`；新 release zip 为 `16883325` bytes，时间 `2026-06-16 14:30:14`，SHA256 `DF146720185BD36A241E492F025901AAD777D197455B9BE2BB534ACC04CF5BEC`；本轮只走后台命令行，未操作鼠标、窗口、热键或桌面画面，因此不声明真实桌面批量 OCR 点击复验 |
| 心流自主沉淀收件箱与默认采纳机制 | completed | 针对 2026-06-16 用户要求“心流需要更高自主性，证据足够就自动生成 Skill/知识/日报/复盘，通知我即可，不删除就默认采纳，删除可输入原因避免下次再生成”：`workmemory.Service` 新增 `AutonomousArtifact`、拒绝原因台账和每日自主流程；自主流程只读取非敏感、已质检证据，生成日报、复盘、知识和低风险 Skill 产物，低风险 Skill 仅在证据足够、流程清晰且所有步骤均无需确认时生成，并标记 `agentExecutable`。删除产物会记录 `dedupKey + reason`，后续同类产物被抑制；心流首页新增自主沉淀收件箱和手动自主整理入口；设置版本迁移到 v11，旧配置会默认开启每日定时草稿/自主检查，但时间机器仍默认关闭。验证：`go test ./internal/workmemory ./internal/settings -v`、清理 AI/Embedding 环境变量后的 `go test ./...`、`pnpm build`、`wails3 task windows:package` 均通过；bindings 为 `469 Packages, 23 Services, 196 Methods, 5 Enums, 183 Models, 0 Events`；新 `bin/ariadne.exe` 为 `32312320` bytes，时间 `2026-06-16 14:17:58`，SHA256 `F47336D76D84766053ACF8806C67491DCFCDB888E6C799F0DA593FEA11FFB7BD`；新 release zip 为 `16882643` bytes，时间 `2026-06-16 14:17:59`，SHA256 `ECF775B7694E5C5EE305AAD126AB7E3059258D41295861DB892940B3586EF3E3`；本轮只走后台命令行，未操作鼠标、窗口、热键或桌面画面，因此不声明真实桌面收件箱点击复验 |
| 心流时间线截图 OCR 自动整理 | completed | 针对 2026-06-16 用户反馈“时间线标题没意义、点进去 OCR 都没做、截图之后应该自动 OCR 并 AI 总结标题和整齐文本”：新配置默认开启 `autoOcr`，截图类工作记忆在质检通过后自动进入 OCR；`ApplyOCRText` 回写时先生成本地整理的 `title/summary/text`，替换 `work`、`截图证据`、`屏幕时间机器` 等泛标题，清理路径/尺寸/来源等噪声，正文生成“画面文字整理/上下文”结构，原始 `ocrText` 保留为证据。AI 配置启用时接入 `OpenAICompatibleOCRSummarizer`，复用 Ariadne AI provider/base/model 和 Credential Manager/API env key，生成更自然的标题、摘要和 Markdown 正文；隐私模式、敏感条目、无模型或 AI 失败时保持本地整理兜底。心流时间线展示优先使用整理后的 `title/summary/text`，原始 OCR 在明细抽屉里折叠为“原始 OCR 证据”，避免列表和详情直接堆乱码 OCR。验证：`go test ./internal/workmemory ./internal/aiclient ./internal/settings -v`、`vue-tsc --noEmit`、`pnpm build`、清理 AI/Embedding 环境变量后的 `go test ./...`、`wails3 task windows:package` 均通过；Vite/Rolldown 仍有第三方 `@vueuse/core` pure annotation 警告和大 chunk 提示，不影响产物；bindings 为 `469 Packages, 23 Services, 193 Methods, 5 Enums, 179 Models, 0 Events`；新 `bin/ariadne.exe` 为 `32207360` bytes，时间 `2026-06-16 13:35:42`，SHA256 `34C1731C786AB39806BDA7544BBB4AF69F212680201DB98DDBDB4AA0D1FAC070`；新 release zip 为 `16841214` bytes，时间 `2026-06-16 13:35:43`，SHA256 `400D2D785BD6920A1B9F414A16BD6973F6CC3BF78585FE7329E765932B8C2C74`；本轮只走后台命令行，未操作鼠标、窗口、热键或桌面画面，因此不声明真实桌面时间线/OCR视觉复验 |
| 网速小窗启动恢复与截图工具栏顺序调整 | completed | 针对 2026-06-16 用户反馈“每次启动都要重新开启网络小窗、截图工具栏的颜色和下面几个高频操作按钮换位置”：`network_mini_window.json` 新增 `visible` 状态，用户打开网速小窗后会持久化，Ariadne 下次启动时 `toolwindows.Attach()` 自动恢复；重置网速小窗位置不再清掉可见状态。截图覆盖层工具栏把 `保存 / 另存 / 复制 / 贴图` 移到原颜色区，颜色选择和粗细控制移到下面原高频操作区。验证：`vue-tsc --noEmit`、`go test ./internal/toolwindows`、`pnpm build`、清理 AI/Embedding 环境变量后的 `go test ./...`、`wails3 task windows:package` 通过；Vite/Rolldown 仍有第三方 `@vueuse/core` pure annotation 警告和大 chunk 提示，不影响产物；bindings 为 `469 Packages, 23 Services, 192 Methods, 5 Enums, 178 Models, 0 Events`；新 `bin/ariadne.exe` 为 `32124928` bytes，时间 `2026-06-16 13:14:51`，SHA256 `DA5F56ED364F9122CAFBAB368BDF4487D6E926F3A0516E93BE152730351E5C34`；新 release zip 为 `16787230` bytes，时间 `2026-06-16 13:14:53`，SHA256 `46DAA73C287CCD0F702F6E62451D1821D544414B55C1AB0F552FA782EDD5786E`；本轮只走后台命令行，未操作鼠标、窗口、热键或桌面画面，因此不声明真实桌面启动恢复/工具栏点击复验 |
| 心流洞察转化动作可解释与生成后跳转处理 | completed | 针对 2026-06-16 用户反馈“这三个操作按钮含义不清，转工作流/转清单没效果，转任务包不知道去哪里看/怎么处理”：洞察卡片按钮改名为 `交给代理`、`生成自动化`、`生成检查清单`，并补 title 说明；三个动作成功后都会自动切到 `资产` 页并高亮对应卡片。资产页外部代理卡新增“下一步”说明、证据/边界/验收数量和 `复制任务包`，用于把任务包交给 Codex 或其他代理处理；工作流/清单卡新增生成后的下一步提示。`workMemory` store 生成函数改为返回成功状态，工作流/清单生成后会保存 `workflow_draft` / `checklist_draft` 洞察状态，后端 `normalizeExperienceDecisionStatus` 已支持这两个状态，洞察页回看会显示 `已生成自动化草稿` / `已生成检查清单`。验证：`vue-tsc --noEmit`、`go test ./internal/workmemory`、`pnpm build`、`go test ./...`、`wails3 task windows:package` 通过；Vite/Rolldown 仍有第三方 `@vueuse/core` pure annotation 警告和大 chunk 提示，不影响产物；bindings 为 `469 Packages, 23 Services, 192 Methods, 5 Enums, 178 Models, 0 Events`；新 `bin/ariadne.exe` 为 `32124928` bytes，时间 `2026-06-16 12:27:45`，SHA256 `11B0CF47D1AA697D6F7485ABCE304AB1D682B72765A46F6977ED68DB430D9C94`；新 release zip 为 `16787088` bytes，时间 `2026-06-16 12:27:46`，SHA256 `76602492634F0A89170B270CB1CBF1D3F720A098CB6E0832C91674613ED1B4A5`；本轮只走后台命令行，未操作鼠标、窗口、热键或桌面画面，因此不声明真实桌面点击复验 |
| 心流进度反馈与 ChatGPT 式首页比例调整 | completed | 针对 2026-06-16 用户反馈“AI归纳需要进度条、时间线选中比较多时删除慢也要状态、心流界面比例参考 ChatGPT 网页”：`workMemory` store 新增 AI 归纳阶段/进度、批量删除 done/total 进度；洞察页确认 AI 归纳时显示阶段文字和进度条，失败或完成都会复位；时间线批量删除时显示正在删除、已完成/总数和进度条。`style.css` 将心流首页从大卡片看板比例收敛为居中的 ChatGPT 式对话列：去掉厚重面板框架，收紧 header 和标题，线程区域占主空间，输入区 sticky bottom，textarea 高度从大面积空框改为更自然的聊天输入高度。验证：`vue-tsc --noEmit`、`pnpm build`、`wails3 task windows:package` 通过；Vite/Rolldown 仍有第三方 `@vueuse/core` pure annotation 警告和大 chunk 提示，不影响产物；bindings 为 `469 Packages, 23 Services, 192 Methods, 5 Enums, 178 Models, 0 Events`；新 `bin/ariadne.exe` 为 `32121344` bytes，时间 `2026-06-16 11:35:53`，SHA256 `5412427049E665F4DDC7B0BFE673E5F2F110C2EAEBDC7484FAC925D5C298CB03`；新 release zip 为 `16786091` bytes，时间 `2026-06-16 11:35:54`，SHA256 `917F2F019E6FF95BBA6254266EB5C6427F55C010FCF1A81DEE2ABB288E58E20A`；本轮只走后台命令行，未操作鼠标、窗口、热键或桌面画面，因此不声明真实桌面视觉/点击复验 |
| 心流时间线 Product Design 重排与按天滚动加载 | completed | 针对 2026-06-16 用户反馈“时间线现在只是普通列表、标题只是来源描述、加载不要一次性全部加载，并要求使用 Product Design”：加载 Product Design context，按“按天回放工作轨迹”的 brief 重排时间线页。`WorkMemoryCenter.vue` 新增按天分组、前 2 天默认加载、IntersectionObserver 滚动哨兵按天追加加载、筛选变化重置加载窗口；事件主标题从摘要/窗口标题/OCR/文本中抽重点，来源程序、来源类型、窗口标题和证据类型退为元信息 chip/badge。`style.css` 将普通列表改成左侧 day marker + 纵向轴线 + 右侧事件卡的时间线样式，并补窄屏单列退化。批量删除、来源程序筛选和复盘入口保留。验证：`vue-tsc --noEmit`、`pnpm build`、`wails3 task windows:package` 通过；Vite/Rolldown 仍有第三方 `@vueuse/core` pure annotation 警告和大 chunk 提示，不影响产物；bindings 为 `469 Packages, 23 Services, 192 Methods, 5 Enums, 178 Models, 0 Events`；新 `bin/ariadne.exe` 为 `32118272` bytes，时间 `2026-06-16 11:23:29`，SHA256 `40B4C4B76F5AF01CF2CDB2183DBEE8803C7E090BC643DD98EBF04C6DA302757F`；新 release zip 为 `16784964` bytes，时间 `2026-06-16 11:23:31`，SHA256 `E9A793AD1314B7DD321EBB5D8D56FFDD81248A0AB26DBB182BDDDAD852D165D0`；本轮只走后台命令行，未操作鼠标、窗口、热键或桌面画面，因此不声明真实桌面滚动/点击复验 |
| 心流时间线批量删除与来源程序筛选 | completed | 针对 2026-06-16 用户要求“时间线页面要增加批量删除功能，按来源程序筛选功能”：`WorkMemoryCenter.vue` 时间线页新增来源程序筛选条，按当前来源类型筛选后的 `appName` 聚合为应用 chip，支持全部程序/单程序筛选叠加；行左侧选择改为时间线批量选择，不再和复盘选择混用；顶部新增选择当前结果、删除选中、清空，删除按钮二次确认且可删除敏感记录；右侧复盘区保留“选中入复盘/当前入复盘/清空复盘/生成复盘”，复盘仍只接收非敏感记录且最多 12 条。`workMemory` store 新增 `selectEntriesForRetrospective(ids)` 和 `deleteEntries(ids)`，批量删除复用现有 `Delete(id)` 后端入口逐条执行，删除后同步本地 entries、选中项、复盘选择、图片预览和搜索结果。验证：`vue-tsc --noEmit`、`pnpm build`、`wails3 task windows:package` 通过；Vite/Rolldown 仍有第三方 `@vueuse/core` pure annotation 警告和大 chunk 提示，不影响产物；bindings 为 `469 Packages, 23 Services, 192 Methods, 5 Enums, 178 Models, 0 Events`；新 `bin/ariadne.exe` 为 `32113152` bytes，时间 `2026-06-16 11:16:12`，SHA256 `ED88F4C8F39C53CE3F3ACE7DB60DC929D54001CF150A1F8A21EE3C7EDAC83252`；新 release zip 为 `16782134` bytes，时间 `2026-06-16 11:16:14`，SHA256 `889D8FC734F3E7A4551F0E95131F69035A25223CF94DF241F7075575800D79A2`；本轮只走后台命令行，未操作鼠标、窗口、热键或桌面画面，因此不声明真实桌面点击复验 |
| 心流聊天建议按钮与输入框点击区域修复 | completed | 针对 2026-06-16 用户反馈“建议按钮有点搞笑、下面输入框看起来很高但只有底部有字处能点”：`WorkMemoryCenter.vue` 给聊天输入 textarea 增加 ref 和容器 pointer-down 聚焦，点击输入框视觉区域任意空白处都会聚焦；`style.css` 新增 `.flow-chat-question-chips`，聊天建议问题改为紧凑 pill，不再复用旧大 ask-card 的圆形/椭圆按钮样式；textarea 撑满输入框高度并保持文本选择/输入区 cursor。验证：`pnpm build`、`wails3 task windows:package` 通过；Vite/Rolldown 仍有第三方 `@vueuse/core` pure annotation 警告和大 chunk 提示，不影响产物；bindings 为 `469 Packages, 23 Services, 192 Methods, 5 Enums, 178 Models, 0 Events`；新 `bin/ariadne.exe` 为 `32109056` bytes，时间 `2026-06-16 11:07:59`，SHA256 `F4B14CD75E389E8B0B50A46C48743DC668BB4B177B2B10EE68D52CD05BF996A2`；新 release zip 为 `16781282` bytes，时间 `2026-06-16 11:08:00`，SHA256 `3A9279C774B2042ECF0FDA53F4BAB56A1306A22B0DBB18C5FD5149CCBC76915E`；本轮只走后台命令行，未操作鼠标、窗口、热键或桌面画面，因此不声明真实桌面点击复验 |
| 时间机器稳定窗口采集、小时级质检与 pending 隔离 | completed | 针对 2026-06-16 用户反馈“默认切到窗口要等 3 秒、窗口保持每 30 秒截一次、多张作为一次采集、每小时自检清理垃圾、总结/提取不处理未质检记录”：默认时间机器改为窗口稳定 `3s` 后才截图，窗口保持期间默认 `30s` 再采一次，不到 3 秒切走的窗口不落库；同一窗口会话的多次截图追加到同一条 `time_machine` 记录的 `frames`，保持 `qualityStatus=pending`，不会进入 OCR、日报、复盘、经验发现、embedding/Milvus、心流问答和 Codex agent evidence。worker 新增每小时自动 `ReviewPendingCaptures()`，跳过当前仍活跃会话，已结束 pending 记录会折叠重复/近似重复帧并只保留最新帧，标记 `checked` 后才允许自动 OCR；当前只实现签名/保守感知指纹重复折叠，尚未实现“最后一张语义包含前面信息”的语义判断。默认设置和前端 fallback 同步为 `autoCaptureIntervalSeconds=30`、`windowSwitchCaptureEnabled=true`、`windowSwitchCooldownSeconds=3`。验证：`go test ./internal/workmemory ./internal/settings`、清理 AI/Embedding 环境变量后的 `go test ./...`、`pnpm build`、`wails3 task windows:package` 通过；Vite/Rolldown 仍有第三方 `@vueuse/core` pure annotation 警告和大 chunk 提示，不影响产物；bindings 为 `469 Packages, 23 Services, 192 Methods, 5 Enums, 178 Models, 0 Events`；新 `bin/ariadne.exe` 为 `32108032` bytes，时间 `2026-06-16 10:58:48`，SHA256 `C58D61B5F0357EC178CB88E302918FD82C2D23174F86F8AA29970036D9143AED`；新 release zip 为 `16781228` bytes，时间 `2026-06-16 10:58:49`，SHA256 `AFDE8D059E2EFF5C2E26575F95BF4A69E11B0E0554CF5FF69BD3E674576B4744`；本轮只走后台命令行，未操作鼠标、窗口、热键或桌面画面，因此不声明真实桌面采集复验 |
| 时间机器应用级采集策略 | completed | 针对 2026-06-16 用户反馈“微信发完消息会切到私人空对话，120 秒自动截图截到的都是空对话”：新增 `WorkMemoryAppCaptureProfile` 配置模型，支持按应用/进程配置启用状态、切换窗口后延迟截图秒数、窗口保持期间采集间隔；settings 版本升至 10，新增归一化、去重和持久化测试。时间机器 worker 改为命中应用策略时由应用策略接管全局定时截图，避免全局 120 秒继续截空窗；应用策略不依赖全局窗口切换触发开关，可单独按目标应用工作；切窗后先记录待触发时间，稳定后截图，驻留期间按应用间隔继续采集；补充测试覆盖延迟切窗截图和命中应用策略时跳过全局间隔。心流设置“采集”页新增“应用采集策略”区块，从最近采集到的应用图标化添加，不再手输进程名；点应用卡片配置详细参数。验证：`go test ./internal/settings ./internal/workmemory -v`、`pnpm build`、`wails3 task windows:package` 通过；Vite/Rolldown 仍有第三方 `@vueuse/core` pure annotation 警告和大 chunk 提示，不影响产物；bindings 为 `469 Packages, 23 Services, 191 Methods, 5 Enums, 176 Models, 0 Events`；新 `bin/ariadne.exe` 为 `32053760` bytes，时间 `2026-06-16 10:28:32`，SHA256 `2F398E3FEE29F5941F480DC6FA2BB20E62D9B236C17544216FC980302557100B`；新 release zip 为 `16761361` bytes，时间 `2026-06-16 10:28:32`，SHA256 `14EC026856CD162A46EC91A32551C8F809BD14636708380E0789017EA1DEDF91`；本轮只走后台命令行，未操作鼠标、窗口、热键或桌面画面，因此不声明真实微信/心流设置点击复验 |
| 心流聊天布局与设置去重/密钥补回 | completed | 针对 2026-06-16 用户反馈“样式错乱、排除规则在规则页面和设置页面重复、设置页面不能设置 AI 接口 key”：修复 `.flow-message` grid 列定位，assistant/system 消息即使没有选择按钮也固定使用头像列 + 气泡列，避免气泡落入 44px 窄列导致文字逐字换行；心流设置的“隐私边界与存储”页签删除排除规则文本区，排除规则仅保留在“规则”页面维护；心流设置“模型与向量”页签补回安全密钥存储，复用 `settings.secretStatus/secretInputs/saveSecret/clearSecret`，支持 AI API key、Embedding key、Milvus token 的保存/清除和环境变量/安全存储状态显示。验证：`pnpm build`、`wails3 task windows:package` 通过；Vite/Rolldown 仍有第三方 `@vueuse/core` pure annotation 警告和大 chunk 提示，不影响产物；bindings 为 `469 Packages, 23 Services, 191 Methods, 5 Enums, 174 Models, 0 Events`；新 `bin/ariadne.exe` 为 `32014336` bytes，时间 `2026-06-16 10:00:21`，SHA256 `4205C6DB49227BD8AFCA6AD5503382702103AFBF82542F2ED652CF220744C752`；新 release zip 为 `16741217` bytes，时间 `2026-06-16 10:00:23`，SHA256 `0681BA4B8CC9D70F42FE3FAE83FBB605A863C8FE8FBD42A039EBCFC4BC731776`；本轮只走后台命令行，未操作鼠标、窗口、热键或桌面画面，因此不声明真实桌面视觉/密钥保存点击复验 |
| 心流首页对话框化与显式沉淀 | completed | 针对 2026-06-16 用户反馈“布局别扭，干脆做成对话框；我与心流的对话；对话记录默认不作为沉淀，可以右键多选后加入”：`WorkMemoryCenter.vue` 首页从大提问卡 + 静态回答卡改为单列聊天线程，提问会追加“我”的消息并通过现有 `AskFlow` 动态生成心流回复；对话消息仅保存在当前前端会话，不自动写入工作记忆；消息支持右键菜单、勾选/多选、复制选中和“加入沉淀”，显式沉淀走新增 `workMemory.addManualNote()` 并以 `manual_note` 写入，标签为 `心流` / `对话沉淀`；证据抽屉保留但默认收起，首页移除右侧摘要/洞察/模型卡片，避免主界面继续像看板。样式层新增 `flow-chat-*`，消息文本和气泡显式 no-drag + user-select，右键菜单 fixed 层展示避免被气泡裁剪。验证：`pnpm build`、`wails3 task windows:package` 通过；Vite/Rolldown 仍有第三方 `@vueuse/core` pure annotation 警告和大 chunk 提示，不影响产物；bindings 为 `469 Packages, 23 Services, 191 Methods, 5 Enums, 174 Models, 0 Events`；新 `bin/ariadne.exe` 为 `32012800` bytes，时间 `2026-06-16 09:44:44`，SHA256 `72A41066FC41157C506EF272130AAC67B5AA5B0683B88A9AFFBCDA3EBA098FEF`；新 release zip 为 `16740458` bytes，时间 `2026-06-16 09:44:45`，SHA256 `193A0688E9214DAB9147633F1314E247A2B3F0053088531C7D771213D3BF821E`；本轮只走后台命令行，未操作鼠标、窗口、热键或桌面画面，因此不声明真实桌面视觉/右键交互复验 |
| 心流问答后端契约、证据绑定与首页接入 | completed | 针对 2026-06-15 用户要求“继续完善心流的功能”：新增 `workmemory.Service.AskFlow` 统一问答入口，支持“今日总结 / 谁找过我 / 工作流优化 / 普通检索”意图识别，回答只使用本地非敏感记忆，统一返回 answer、intent、mode、suggestedQuestions 和本次问题对应的 evidence；前端 `workMemoryApi`、Pinia store 和心流首页改为调用该入口，不再在组件里分散调用日报、语义搜索和经验发现；证据抽屉优先展示本次问答命中的记忆，回答区复制文本和子元素显式 `no-drag`，避免拖选文字时拖动窗口；新增 Go 测试覆盖今日总结、沟通类问题和敏感记录不进入回答/证据。验证：`go test ./internal/workmemory -count=1`、`go test ./...`、`pnpm build`、`wails3 generate bindings -clean=false`、`wails3 task windows:package` 通过；Vite/Rolldown 仍有第三方 `@vueuse/core` pure annotation 警告和大 chunk 提示，不影响产物；bindings 为 `469 Packages, 23 Services, 190 Methods, 5 Enums, 173 Models, 0 Events`；新 `bin/ariadne.exe` 为 `31979008` bytes，时间 `2026-06-15 23:50:03`，SHA256 `2E65377CFBA76566F8C4F1FC724553B84B37BBB730D5549E00F826C49A9F9551`；新 release zip 为 `16728728` bytes，时间 `2026-06-15 23:50:04`，SHA256 `115EDA3D2492384A9C6644144953DB521A3434B0EF38D66350140A92BA44047B`；本轮只走后台命令行，未操作鼠标、窗口、热键或桌面画面，因此不声明真实桌面问答点击复验 |
| 心流时间线去工作台化与心流设置分段重排 | completed | 针对 2026-06-15 用户反馈“时间线页面就是把原来的工作记忆中心塞进来了、乱糟糟没有主次、心流设置做得敷衍且叠压”：`WorkMemoryCenter.vue` 删除时间线页旧三栏工作记忆结构，改为时间线 header、四项统计、来源筛选、最近轨迹列表和右侧轻量复盘/时间机器/今日来源摘要；明细、OCR 图片和文本证据默认不常驻，点击轨迹后才打开证据抽屉；手动笔记、语义索引、数据包、排除规则、日报/知识/任务包等低频模块继续留在草稿/资产/规则等独立页，不再塞进时间线。心流设置抽屉改为状态概览 + 采集/模型/边界三段页签，修复原 grid 容器导致多个 section 叠压的问题，抽屉改为 header/body/footer 三段，body 独立滚动。样式层新增 `flow-timeline-*`、`flow-quiet-panel`、`flow-settings-overview/tabs/body`，并补齐窄屏响应式。验证：`pnpm build`、`go test ./...`、`wails3 task windows:package` 通过；Vite/Rolldown 仍有第三方 `@vueuse/core` pure annotation 警告和大 chunk 提示，不影响产物；bindings 为 `469 Packages, 23 Services, 189 Methods, 5 Enums, 170 Models, 0 Events`；新 `bin/ariadne.exe` 为 `31920128` bytes，时间 `2026-06-15 22:56:54`，SHA256 `2DEA34B0B4410BAC3AA63B09F3EE43C1EE0624F1A660E5846850B71FA1A7A7E7`；新 release zip 为 `16697550` bytes，时间 `2026-06-15 22:56:54`，SHA256 `6A9B878B0388F686DCECCA1977E46BB71737DD3022AAD5E5DF56284053802A2B`；本轮按要求只走后台命令行，未操作鼠标、窗口、热键或桌面画面，因此不声明真实桌面视觉复验 |
| 心流侧栏、回答复制、心流设置抽屉和普通窗口缩放修复 | completed | 针对 2026-06-15 用户反馈“侧边菜单没有字/logo 缺失、心流回答没有叙事结构且不能复制、鼠标拖选会拖动窗口、窗口除最大化外不能拖拽边框调节大小”：`WorkMemoryCenter.vue` 恢复带 Ariadne 真实图标、文字和副标题的心流侧栏，移除窄窗口自动折叠为纯图标栏；心流回答改为摘要段落 + 主线/证据/洞察三段叙事结构，新增“复制回答”，回答区可文本选择且不再拖动窗口；侧栏“设置”改为心流内设置抽屉，集中维护采集、自动整理、AI/Embedding/Milvus、隐私排除和存储保留；设置中心可见导航移除工作记忆、AI 与向量、隐私规则入口，数据页不再放心流保留配置；普通工具窗口恢复可拖拽边框缩放，启动器/截图遮罩/贴图/网速小窗仍保持特殊窗口策略。验证：`pnpm --dir experiments\ariadne\frontend exec vue-tsc --noEmit`、`pnpm --dir experiments\ariadne\frontend build`、`go test ./internal/toolwindows -v`、`go test ./...`、`wails3 task windows:package` 通过；Vite/Rolldown 仍有第三方 `@vueuse/core` pure annotation 警告和大 chunk 提示，不影响产物；bindings 为 `469 Packages, 23 Services, 189 Methods, 5 Enums, 170 Models, 0 Events`；新 `bin/ariadne.exe` 为 `31934464` bytes，时间 `2026-06-15 22:36:46`，SHA256 `A545B6DB85BA4716A8A7BA0B064F4708859F40FBEA8F24B4617DEB4EC6617CC7`；新 release zip 为 `16699803` bytes，时间 `2026-06-15 22:36:47`，SHA256 `85BF1F7B4F485499A328E495659B8FE7975582E399FB433F3D1E33B2E8F1307D`；本轮只走后台命令行，未操作鼠标、窗口、热键或桌面画面，因此不声明真实桌面视觉/拖拽复验 |
| 心流首页贴近 Product Design 设计图二次修正 | completed | 针对 2026-06-15 用户反馈“和设计图差距有点大”：把 `WorkMemoryCenter.vue` 默认首页进一步从旧式全宽看板收敛到设计图结构，入口变为左侧垂直导航 + 顶部居中状态胶囊 + 中央大对话框 + 回答卡 + 右侧今日摘要/可优化流程/连接与模型卡片；隐藏旧底部状态条，原始证据继续默认收起；顶部运行控制压缩为图标级控件，减少工具条感；样式层调整 `flow-*` 左栏宽度、顶部留白、问答卡高度、主栏/右栏比例和卡片阴影，使首屏更接近参考图。验证：`git diff --check -- experiments/ariadne/frontend/src/components/workmemory/WorkMemoryCenter.vue experiments/ariadne/frontend/src/style.css`、`pnpm --dir experiments\ariadne\frontend exec vue-tsc --noEmit`、`pnpm --dir experiments\ariadne\frontend build`、`go test ./...`、`wails3 task windows:package` 通过；Vite/Rolldown 仍有第三方 `@vueuse/core` pure annotation 警告和大 chunk 提示，不影响产物；bindings 为 `469 Packages, 23 Services, 189 Methods, 5 Enums, 170 Models, 0 Events`；新 `bin/ariadne.exe` 为 `31915008` bytes，时间 `2026-06-15 22:13:43`，SHA256 `DDA13649357D46653A23A5ED23F4EE789E625B82D299BBA03B60E952B9439D24`；新 release zip 为 `16695757` bytes，时间 `2026-06-15 22:13:44`，SHA256 `5B8CAF01B5D29B19EE5DC071E2B160F1D08361337B08D63863AE72D2B84C084C`；本轮只走后台命令行，未操作鼠标、窗口、热键或桌面画面，因此不声明真实桌面视觉复验 |
| 工作记忆中心重设计为“心流”对话式第二大脑 | completed | 针对 2026-06-15 用户反馈“工作记忆中心界面乱、明细不应和高频操作混在一起、第二大脑不要制造决策负担、改名为心流”：按 Product Design 方向 `1+2` 将 `WorkMemoryCenter.vue` 默认入口从三栏记录/详情/侧栏重构为“心流”首页，主交互改为对话框，可直接问“我今天干了些什么”“今天有哪些人找过我”“今天我的哪些工作流可以优化”；今日摘要、自动洞察和证据计数在首页轻量展示，原始证据默认收起并通过证据抽屉/时间线按需打开；洞察、草稿、资产、规则拆为独立页，经验发现不再在主界面展示接受/驳回决策，只在需要转任务包/工作流/清单/Skill 等落地动作时确认；新增右侧证据明细抽屉，避免把当前选中记录和全局高频操作混在同一面板。样式层新增 `flow-*` 视觉系统，保留 Graphite Teal 浅色基调、清晰外框线和内部区域边界。验证：`pnpm --dir experiments\ariadne\frontend exec vue-tsc --noEmit`、`pnpm --dir experiments\ariadne\frontend build`、`go test ./...`、`wails3 task windows:package` 通过；Vite/Rolldown 仍有第三方 `@vueuse/core` pure annotation 警告和大 chunk 提示，不影响产物；bindings 为 `469 Packages, 23 Services, 189 Methods, 5 Enums, 170 Models, 0 Events`；新 `bin/ariadne.exe` 为 `31911936` bytes，时间 `2026-06-15 21:56:03`，SHA256 `EF5B5CC813EE6272AA46F48F14038F7F8C0841A46F728187F86B23029EB7ECE8`；新 release zip 为 `16694930` bytes，时间 `2026-06-15 21:56:04`，SHA256 `B418BB33E698033A8EAD1F9EACA3EE80F959655FE89238A31386565E956E9D2F`；本轮只走后台命令行，未操作鼠标、窗口、热键或桌面画面，因此不声明真实桌面视觉复验 |
| 普通工具窗口最大化不覆盖任务栏与标准最大化/还原图标 | completed | 针对 2026-06-15 用户反馈“窗口最大化之后任务栏什么都点不了，好像挡着了，最大化和还原按钮为什么是全屏 icon”：确认 `WindowControls.vue` 之前直接调用 Wails `Window.ToggleMaximise()`，在当前无边框/透明壳组合下会把窗口铺到完整屏幕区域，导致透明窗口区域盖住 Windows 任务栏；同时使用 `Maximize2` 对角箭头图标，视觉上像全屏而不是标准窗口最大化。本轮改为自管窗口最大化状态：点击最大化时读取当前 `Window.Position()`/`Window.Size()` 保存还原 frame，再读取 `Window.GetScreen().WorkArea`，用 `Window.SetPosition(workArea.x, workArea.y)` + `Window.SetSize(workArea.width, workArea.height)` 手动铺满当前工作区，不再覆盖任务栏；点击还原时恢复保存的 frame，缺失 frame 时按当前 WorkArea 居中回到 1120x720 附近默认尺寸；同时处理已处于原生 maximised 状态的窗口先 `Restore()`。图标改为标准单方框最大化和 CSS 双重方框还原，保留最小化和关闭。验证：`pnpm --dir experiments\ariadne\frontend exec vue-tsc --noEmit`、`pnpm --dir experiments\ariadne\frontend build`、`go test ./...`、`wails3 task windows:package` 通过；Vite/Rolldown 仍有第三方 `@vueuse/core` pure annotation 警告和大 chunk 提示，不影响产物；bindings 为 `469 Packages, 23 Services, 189 Methods, 5 Enums, 170 Models, 0 Events`；新 `bin/ariadne.exe` 为 `31875072` bytes，时间 `2026-06-15 21:15:37`，SHA256 `2F0069D9179A72D720B9D3A211ADE941D0E1A1B57B8CDBB631C076C4F32DA052`；新 release zip 为 `16686983` bytes，时间 `2026-06-15 21:15:38`，SHA256 `B9DF3AF80F37417FAC4439272ABF7F93756991D059CAA4874F4D9B6E2A43AD53`；本轮只走后台命令行，未操作鼠标、窗口、热键或桌面画面，因此不声明真实任务栏点击视觉复验 |
| OCR Python 弹窗、中文乱码和旧 x-tools venv 依赖修复并重新打包 | completed | 针对 2026-06-15 用户反馈“OCR 是否调用 x-tools、最终不应依赖 x-tools、中文全是乱码、一直弹 Python 窗口”：确认 Ariadne OCR 是 `internal/ocr` 的 RapidOCR bridge，不是调用旧 x-tools UI，但此前会默认查找仓库根 `.venv\Scripts\python.exe`，且 Windows GUI 程序启动控制台版 Python 时未设置无窗口创建，导致弹出黑色控制台；Python bridge 又用 `ensure_ascii=false` 输出 JSON 但未强制 UTF-8 stdout，导致中文被系统编码污染。修复：Windows OCR 子进程设置 `HideWindow` + `CREATE_NO_WINDOW`；子进程环境强制 `PYTHONIOENCODING=utf-8` 和 `PYTHONUTF8=1`；bridge 启动时重配 stdout/stderr 为 UTF-8，并把 RapidOCR 第三方 stdout 重定向到 stderr，保证 stdout 只输出 UTF-8 JSON；OCR Python 查找顺序改为 `ARIADNE_OCR_PYTHON`、`ARIADNE_OCR_HOME`、Ariadne 安装目录/runtime、`%LOCALAPPDATA%\Ariadne\ocr-python`、系统 `python`，仓库 `.venv` / `uv` 仅在显式 `ARIADNE_OCR_ALLOW_REPO_PYTHON=1` 开发模式下启用，且不接受 `pythonw.exe` 这类无 stdout 的解释器；release manifest/README 改为说明核心包独立于旧 x-tools/PyQt/PyInstaller，OCR 需要显式 Ariadne OCR runtime。验证：`go test ./internal/ocr -v`、`go test ./internal/ocr ./internal/releasepack -v`、`go test ./...`、`py_compile rapidocr_bridge.py`、`wails3 task windows:package` 通过；Vite/Rolldown 仍有第三方 `@vueuse/core` pure annotation 警告和大 chunk 提示，不影响产物；bindings 为 `469 Packages, 23 Services, 189 Methods, 5 Enums, 170 Models, 0 Events`；新 `bin/ariadne.exe` 为 `31873024` bytes，时间 `2026-06-15 17:48:47`，SHA256 `73A39FEDCA9524F0B31EB2F3A08EDB9346CA59A35D404674EBE65252007D75EE`；新 release zip 为 `16685764` bytes，时间 `2026-06-15 17:48:48`，SHA256 `4501DBD416054946A4710A6DF4C2DD61795D92FD768D7E7F3B2A38D37D535B31`；本轮只走后台命令行，未操作桌面画面、鼠标、热键或窗口，因此未做真实 OCR 点击复验 |
| 工作记忆主动沉淀桥接并重新打包 | completed | 针对 2026-06-15 用户要求“继续完善主动沉淀”：剪贴板历史和截图历史新增 entry observer，主进程把剪贴板监听、截图历史入库事件桥接到工作记忆；开启工作记忆且非隐私模式，并启用对应来源开关时，文本/图片剪贴板会以 `clipboard_text`/`clipboard_image` 进入工作记忆，截图历史会以 `screenshot` 进入工作记忆，按 signature/content hash 去重，并跳过 `manual_capture`、`time_machine` 等已由工作记忆自身写入的来源，避免重复沉淀。平台 `remember` action 现在对剪贴板/截图结果真实写入工作记忆，不再伪成功；工作记忆中心新增“开启主动沉淀”入口，只开启剪贴板和截图历史来源，不静默开启屏幕时间机器。新增/更新测试覆盖剪贴板 observer、截图 observer、工作记忆去重/跳过、平台 remember handler；验证：`go test ./internal/workmemory -v`、`go test ./internal/clipboardhistory ./internal/capturehistory ./internal/platform -v`、`go test ./...`、`pnpm --dir experiments\ariadne\frontend exec vue-tsc --noEmit`、`pnpm --dir experiments\ariadne\frontend build`、`wails3 task windows:package` 通过；Vite/Rolldown 仍有第三方 `@vueuse/core` pure annotation 警告和大 chunk 提示，不影响产物；bindings 为 `469 Packages, 23 Services, 189 Methods, 5 Enums, 170 Models, 0 Events`；新 `bin/ariadne.exe` 为 `31864320` bytes，时间 `2026-06-15 17:39:11`，SHA256 `9DFDC41A3B377C5670C6DCA3FBD34FD3F2381E81FA40DDD9666CBA6423B3323F`；新 release zip 为 `16682861` bytes，时间 `2026-06-15 17:39:12`，SHA256 `5DB286D7273C0EB2A1E8CCDC0AB2F67C51D4BB57A91E1D74A605845F2D194348`；本轮只走后台命令行，未操作桌面画面、鼠标、热键或窗口 |
| 设置中心铺满态外框线条补回 | completed | 针对 2026-06-15 用户澄清“我指的是外框线条没有”：确认上一轮为去掉居中卡片壳，把 `html.window-controls-visible .launcher-shell` 设成 `border: 0`，导致设置中心最外层窗口轮廓缺失。本轮仅补回铺满态外轮廓：`border: 1px solid var(--border-strong)` 并加很轻的 inset 线，继续保持 `border-radius: 0`、不恢复大阴影/浮动卡片。验证：`pnpm --dir experiments\ariadne\frontend exec vue-tsc --noEmit`、`pnpm --dir experiments\ariadne\frontend build`、`go test ./...`、`wails3 task windows:package` 通过；Vite/Rolldown 仍有第三方 `@vueuse/core` pure annotation 警告和大 chunk 提示，不影响产物；bindings 为 `469 Packages, 23 Services, 187 Methods, 5 Enums, 170 Models, 0 Events`；新 `bin/ariadne.exe` 为 `31835136` bytes，时间 `2026-06-15 17:01:32`，SHA256 `3FB8F707D3B5DAC9B6B052E877BE557A21CFF28D6FF7F899916BB7E717CB9DE8`；新 release zip 为 `16668663` bytes，时间 `2026-06-15 17:01:33`，SHA256 `7608387964FB466540D7ACF8453FF98938E947F64EE4F962110B28BEAA43898A`；本轮只走后台命令行，未操作桌面画面、鼠标、热键或窗口 |
| 设置中心顶部冗余按钮删除与铺满态结构线增强 | completed | 针对 2026-06-15 用户反馈“缺了边框线条，都和背景融为一体了，右上角的保守默认、启动器两个按钮可以删掉”：删除设置中心 header 右侧私有的“保守默认”pill 和“启动器”按钮，仅保留 App 层普通窗口控制；保留其他工具中心各自返回启动器入口不变。普通中心铺满态下补清晰结构线：header/tool-header 使用 `border-strong` 底线和 surface 背景，settings workspace 用更强分隔线背景，左侧 rail 加右边框，settings summary/panel/page header 统一加强边框，footer/status strip 加顶部分隔线和 raised 背景；不恢复居中卡片壳、圆角、大阴影或 blur。验证：`pnpm --dir experiments\ariadne\frontend exec vue-tsc --noEmit`、`pnpm --dir experiments\ariadne\frontend build`、`go test ./...`、`wails3 task windows:package` 通过；Vite/Rolldown 仍有第三方 `@vueuse/core` pure annotation 警告和大 chunk 提示，不影响产物；bindings 为 `469 Packages, 23 Services, 187 Methods, 5 Enums, 170 Models, 0 Events`；新 `bin/ariadne.exe` 为 `31835136` bytes，时间 `2026-06-15 16:56:26`，SHA256 `2AFA66098E2432154DBCADBE485EF1C70FD37322B53208ECCBE6BD696388112F`；新 release zip 为 `16668759` bytes，时间 `2026-06-15 16:56:27`，SHA256 `2C15C2CC1BA4CD3128F8CFC869CA7A02FCFFA45AE8663278B03A8676607B3AB8`；本轮只走后台命令行，未操作桌面画面、鼠标、热键或窗口 |
| 设置中心最大化铺满、壳边框移除和启动项点击保护 | completed | 针对 2026-06-15 用户反馈“最大化非常丑陋，非最大化状态也有一圈丑陋的边框，Everything 依旧点不动”：确认设置中心当前常在主无边框窗口 active view 内显示，上一轮只处理 `tool-window-document` 不足以覆盖主壳 fallback。本轮将 `html.window-controls-visible` 下的普通中心视图改为 `100vw/100vh` 铺满窗口，`.app-frame` 不再居中卡片，`.launcher-shell` 在普通中心视图中去除边框、圆角、阴影和 blur，最大化/非最大化都不再出现中间浮动壳；窗口控制条改为轻量标题栏控件，取消外框/阴影，靠近右上角。启动项列表补 `data-no-drag`、`@pointerdown.stop` 和显式 `--wails-draggable: no-drag`，点击后通过 `settings.showFeedback()` 显示“已选择启动项：...”以便确认事件触发；同时保持 `selectLauncher()` 更新 draft 并重置滚动。验证：`pnpm --dir experiments\ariadne\frontend exec vue-tsc --noEmit`、`pnpm --dir experiments\ariadne\frontend build`、`go test ./...`、`wails3 task windows:package` 通过；Vite/Rolldown 仍有第三方 `@vueuse/core` pure annotation 警告和大 chunk 提示，不影响产物；bindings 为 `469 Packages, 23 Services, 187 Methods, 5 Enums, 170 Models, 0 Events`；新 `bin/ariadne.exe` 为 `31834624` bytes，时间 `2026-06-15 16:49:38`，SHA256 `684744C7A302ADA98145FEE9F64E2977850D40A7D92A6D0EB2CE4EFCD4D4DD7F`；新 release zip 为 `16668710` bytes，时间 `2026-06-15 16:49:39`，SHA256 `B1BCBFD42BBF56B4589BB22D723D7C8D29DA04A557C8F7A0533A3A98D55AF263`；本轮只走后台命令行，未操作桌面画面、鼠标、热键或窗口 |
| 设置中心窗口控制、截图热键对齐、启动项选择和切页滚动修复 | completed | 针对 2026-06-15 用户反馈“还是没有最小化/最大化/还原/关闭，截图这里没对齐，启动项 Everything 点了没反应，滚动条状态会带到下一页”：新增 `WindowControls.vue`，在 App 层只对普通工具中心显示最小化、最大化/还原、关闭控件，启动器、截图覆盖层、贴图和网速小窗不显示；关闭行为复用 `appShell.closeCurrentWindow()`，独立工具窗关闭，主壳 fallback 返回启动器。设置中心右侧内容加 `settingsContentRef`，切换分类和选择启动项时重置右侧滚动到顶部；启动项列表按钮明确 `type="button"` 并走 `selectLauncher()`，避免点击被默认行为或拖拽层吞掉；快捷键页的 `settings-hotkey-grid settings-policy-grid` 改为三等列，贴图热键与主窗口/截图对齐。验证：`pnpm --dir experiments\ariadne\frontend exec vue-tsc --noEmit`、`pnpm --dir experiments\ariadne\frontend build`、`go test ./...`、`wails3 task windows:package` 通过；Vite/Rolldown 仍有第三方 `@vueuse/core` pure annotation 警告和大 chunk 提示，不影响产物；bindings 为 `469 Packages, 23 Services, 187 Methods, 5 Enums, 170 Models, 0 Events`；新 `bin/ariadne.exe` 为 `31834112` bytes，时间 `2026-06-15 16:41:59`，SHA256 `B9B83FAAAFC26B275807E984496BC3EA4C83DA143D330DCC97443A7BB2A49319`；新 release zip 为 `16668763` bytes，时间 `2026-06-15 16:42:00`，SHA256 `ECD2CA33662D2299E706ACCF943F893238E5039E5A45885D5D6B6A21C67DEA0A`；本轮只走后台命令行，未操作桌面画面、鼠标、热键或窗口 |
| 设置中心产品设计重排与普通工具窗原生窗口控制 | completed | 针对 2026-06-15 用户反馈“太乱了，什么都挤在一个页面”以及“窗口还是应该要有最小化最大化还原关闭这些按钮的，只不过启动器这类特殊窗口不要”：按 Product Design brief 将设置中心从单页堆叠改为左侧任务导航 + 右侧单一 active page，分为通用、快捷键、插件、启动项、截图、工作记忆、AI 与向量、隐私规则、数据与存储、高级维护；默认页不再展示迁移/兼容/诊断噪音，插件配置保留独立页，高级维护收纳平台诊断、数据导入和回滚检查点。普通 tool-window 改用系统标题栏和原生最小化/最大化/还原/关闭，`network-mini` 继续无边框；前端 standalone 普通工具窗调用 `ensureUtilityWindow(false, false, false)`，截图覆盖层、贴图、网速小窗等特殊窗口仍保持专用 frameless/transparent/topmost 策略。新增 `TestOnlyNetworkMiniKeepsUtilityWindowChrome` 锁住窗口分界；验证：`pnpm --dir experiments\ariadne\frontend exec vue-tsc --noEmit`、`pnpm --dir experiments\ariadne\frontend build`、`go test ./internal/toolwindows -v`、`go test ./...`、`wails3 task windows:package` 通过；Vite/Rolldown 仍有第三方 `@vueuse/core` pure annotation 警告和大 chunk 提示，不影响产物；bindings 为 `469 Packages, 23 Services, 187 Methods, 5 Enums, 170 Models, 0 Events`；新 `bin/ariadne.exe` 为 `31832064` bytes，时间 `2026-06-15 16:28:50`，SHA256 `8D84CE2283DAB93553C34BD4F55F2B7BD24FF0E0B8BE168DF8E716A95DF47051`；新 release zip 为 `16668498` bytes，时间 `2026-06-15 16:28:51`，SHA256 `5B30C743D3E66111F2A4258EE24F2E9706A2D081C43BBC759EB53D02F76DB7DD`；本轮只走后台命令行，未操作桌面画面、鼠标、热键或窗口 |
| 设置中心插件配置可见化与兼容信息降噪 | completed | 针对 2026-06-15 用户反馈“还是一堆兼容说明，到现在没看到插件配置”：设置中心左侧摘要移除旧版配置/旧历史/旧版运行常驻 meter，底部状态条移除旧版迁移提示，配置存储、平台诊断、搜索数据、数据导入、数据保护改为默认折叠维护区；主表单在快捷键下方新增“插件”配置区，直接读取 Go `plugins.List()` manifest，展示插件名称、描述、用法、关键词和能力，并通过 `settings.plugins.enabled[id]` 启停插件；正式替换后不再注册旧 Python 插件桥。后端 `plugins.Service` 新增运行时 enabled map，搜索触发器和 `Execute` 均尊重停用状态，设置保存后通过 settings change handler 同步；settings 归一化插件 id 为小写，避免旧配置 `UUID`/`JSON` 导入后导致状态和运行时不一致。新增 `TestPluginEnabledSettingsHideTriggersAndBlockExecution`，更新旧配置导入测试；验证：`vue-tsc --noEmit`、`go test ./internal/plugins ./internal/settings -v`、`pnpm --dir frontend build`、`go test ./...`、`wails3 task windows:package` 通过；并行跑 `go test ./...` 与 Vite build 曾因 `frontend/dist` 被重写导致 Go embed 旧 hash 文件缺失，串行重跑已通过；bindings 为 `469 Packages, 23 Services, 187 Methods, 5 Enums, 170 Models, 0 Events`；新 `bin/ariadne.exe` 为 `31819776` bytes，时间 `2026-06-15 14:16:11`，SHA256 `31B09ED363217ABBDFBA21BE0DE066ACEED9955E9EC7A7677C9F5B22E1D9BEE9`；新 release zip 为 `16667018` bytes，时间 `2026-06-15 14:16:12`，SHA256 `1BFE15668C6E198430D7A56A8125AF45538ED401431E4ACAB259EC5646A1F4C1`；本轮只走后台命令行，未操作桌面画面 |
| 设置中心隐藏已完成迁移入口并补齐配置项 | completed | 针对 2026-06-15 用户反馈“设置中心左边一堆迁移相关，数据已迁移好就不要再出现 x-tools；设置中心少了很多配置项”：后端 `LegacyConfigStatus` 和 `LegacyDataStatus` 新增 `needsImport`，配置迁移通过旧配置映射结果与当前 Ariadne 设置/安全凭据对比判断是否还需导入，历史迁移通过剪贴板/截图/工作记忆中明确 `legacy_x_tools` 的导入记录数判断是否还有旧源未导入；设置中心只在旧配置仍需导入、旧历史仍有未导入项、旧版进程/热键冲突或刚产生迁移结果时显示旧版 x-tools 面板，左侧摘要和底部状态条也不再常驻旧版信息；回滚检查点移出旧版面板，作为独立“数据保护”面板保留。补齐 UI 中已有后端字段：语言、截图自动保存、截图质量、工作记忆截图质量、经验发现天数、Skill/工作流建议、外部代理任务包、OpsCore 同步、外部代理任务目录、trace、收藏永久保留等。新增/更新测试覆盖配置迁移导入后 `needsImport=false`、历史迁移导入后各源不再需要迁移；验证：`go test ./internal/settings ./internal/migration ./internal/clipboardhistory ./internal/capturehistory ./internal/workmemory -v`、`go test ./...`、`pnpm --dir frontend build`、`wails3 task windows:package` 通过；bindings 为 `469 Packages, 23 Services, 186 Methods, 5 Enums, 170 Models, 0 Events`；新 `bin/ariadne.exe` 为 `31803904` bytes，时间 `2026-06-15 13:17:50`，SHA256 `BA84913974BE00F823209859FD1EE41D67950F8961E999136C0E2997B03805F4`；新 release zip 为 `16660599` bytes，时间 `2026-06-15 13:17:52`，SHA256 `769247A677782721DCF8C939DF518601CCC79E24D64F88D4F9E38875ACCB74F8`；本轮按用户限制只走后台命令行，未操作鼠标、窗口、热键或桌面画面，因此不声明真实设置中心视觉复验 |
| 网速小窗持续置顶闪烁修复并重新打包 | completed | 针对 2026-06-15 用户反馈“重置时小窗会跑到屏幕中间一瞬间、无遮挡也不断重置导致左下角小窗一直闪烁”：确认上一轮把完整 `applyNetworkMiniPlacement()` 放进 900ms monitor tick，导致每次 tick 都执行 `SetScreen/SetSize/SetRelativePosition`，Wails 会出现可见重排闪烁。本轮拆分“定位”和“层级保活”：打开、配置变化、全屏自动隐藏恢复时仍使用完整定位；常规 tick 只调用 `refreshNetworkMiniTaskbarLayer()`，执行不移动不改尺寸的 Win32 owner/topmost 保活，且不带 `SWP_SHOWWINDOW`，避免周期性重排和闪烁；Windows/非 Windows taskbar helper 均补齐对应入口。验证：`go test ./internal/toolwindows -v`、`go test ./...`、`wails3 task windows:package` 通过；bindings 为 `469 Packages, 23 Services, 183 Methods, 5 Enums, 170 Models, 0 Events`；新 `bin/ariadne.exe` 为 `31787008` bytes，时间 `2026-06-15 11:14:17`，SHA256 `E0CC45B673283D1ABB0F840B6CB6586E38839790F4F927E5C4CE2657099F5A6C`；新 release zip 为 `16649728` bytes，时间 `2026-06-15 11:14:18`，SHA256 `432237F325EFBAE764F7ABCB79FE06B7B92845E93AE27062D86CA4146D15581C`；本轮按用户限制只走后台命令行，未操作鼠标、窗口、热键或桌面画面，因此不声明真实闪烁视觉复验 |
| 网速小窗任务栏层级持续置顶修复并重新打包 | completed | 针对 2026-06-15 用户反馈“网速小窗的层级没有任务栏高”：复核旧版 `src/ui/network_monitor.py`，旧 x-tools 使用 `ToolTip` 小窗并在 `showEvent()` 和每次 `update_traffic()` 中持续 `anchor_to_taskbar()` + `keep_on_top()`，后者会重复 `SetWindowLongPtrW(GWLP_HWNDPARENT=Shell_TrayWnd)` 与 `SetWindowPos(HWND_TOPMOST)`。Ariadne 之前只在打开/配置变化时应用一次任务栏 owner/topmost，容易被任务栏后续 Z-order 压住。本轮改为网络小窗可见时由 900ms monitor tick 持续重算任务栏位置并恢复 owner/topmost；Windows native 层补 `WS_EX_TOOLWINDOW | WS_EX_TOPMOST | WS_EX_NOACTIVATE`，移除 `WS_EX_APPWINDOW`，继续使用 `SWP_NOACTIVATE` 避免周期性抢焦点。验证：`go test ./internal/toolwindows -v`、`go test ./...`、`wails3 task windows:package` 通过；bindings 为 `469 Packages, 23 Services, 183 Methods, 5 Enums, 170 Models, 0 Events`；新 `bin/ariadne.exe` 为 `31785984` bytes，时间 `2026-06-15 11:02:19`，SHA256 `9B241F89ABA767C8FCBD1AEAFEA7A0C1CF6473ED2ADDFF7D2D176931A8C7888C`；新 release zip 为 `16649084` bytes，时间 `2026-06-15 11:02:19`，SHA256 `61E63E969EE4847708144A91920590C159BF0614E6BC2D105D07C4292CA9A64E`；本轮按用户限制只走后台命令行，未操作鼠标、窗口、热键或桌面画面，因此不声明真实任务栏层级视觉复验 |
| 贴图全局快捷键注入与截图工具栏高频动作右移并重新打包 | completed | 针对 2026-06-15 用户反馈“贴图为什么不注入、截图工具栏最左四个应移到最右、贴图和复制应靠近鼠标位置”：shell manager 从主窗口/截图两个全局热键扩展为主窗口/截图/贴图三个热键，`ApplyHotkeys`、`RetryHotkeyRegistration`、运行态 `ShellStatus` 和设置中心状态 pill 均新增贴图热键；贴图热键触发 `pinnedImageService.OpenCurrentClipboard()`，会读取当前系统剪贴板，图片直接创建贴图，文本生成轻量 SVG 文本贴图并支持右键复制文本；设置页不再显示“仅保存/暂未注册”，重复快捷键校验扩展到三者互斥；截图覆盖层工具栏把原最左侧四个动作移到最右侧，顺序调整为低频编辑在左、保存/另存/复制/贴图在右，复制和贴图靠近右端鼠标区域。新增 `TestOpenCurrentClipboardImageStagesPinnedImage`、`TestOpenCurrentClipboardTextStagesTextPinnedImage`；验证：`go test ./internal/shell ./internal/pinnedimage ./internal/clipboardhistory ./internal/platform -v`、`go test ./...`、`wails3 generate bindings -clean=false`、`pnpm --dir frontend build`、`wails3 task windows:package` 通过；bindings 为 `469 Packages, 23 Services, 183 Methods, 5 Enums, 170 Models, 0 Events`；新 `bin/ariadne.exe` 为 `31784960` bytes，时间 `2026-06-15 10:46:46`，SHA256 `FCFAAF10F687ACE7D620FF6ACC028239BFB47326E888D49F5C7B933B566DC421`；新 release zip 为 `16648273` bytes，时间 `2026-06-15 10:46:47`，SHA256 `0AF49B4D2BBB1AECC6C5019BD37AFC9B8094841BB6555C34AF4696096998DBF7`；本轮仍按用户要求只走后台命令行，未做真实桌面热键/贴图/工具栏点击复验 |
| F1 截图快捷键、截图不隐藏自身窗口、任务栏网速小窗修复并重新打包 | completed | 针对 2026-06-15 用户反馈“截图快捷键不能设置为 F1、截图时会隐藏工具本身、网速监控没有嵌入任务栏”：后端热键解析允许裸 `F1`-`F24`，仍拒绝裸字母/数字/空格；设置页按键捕获和文本规范化同步允许裸功能键并阻止浏览器 F1 帮助抢占；截图覆盖层 `Open()` 改为直接截图后再开 overlay，不再先隐藏 Ariadne 自有窗口，`captureWindowShouldHide()` 当前固定返回 false；网络小窗改为 x-tools 风格任务栏左侧小条，默认 `taskbar-left`、`156x40`、透明背景、无 header/footer，打开时不再 `Focus()` 抢焦点，Windows 下通过 `Shell_TrayWnd` + `SetWindowLongPtrW(GWLP_HWNDPARENT)` + `SetWindowPos(HWND_TOPMOST|SWP_NOACTIVATE)` 挂到任务栏层级；保留旧四角 anchor 兼容。验证：`go test ./internal/shell ./internal/captureoverlay ./internal/toolwindows -v`、`go test ./...`、`pnpm --dir frontend build`、`wails3 task windows:package` 通过；bindings 为 `469 Packages, 23 Services, 181 Methods, 5 Enums, 169 Models, 0 Events`；新 `bin/ariadne.exe` 为 `31770624` bytes，时间 `2026-06-15 10:35:17`，SHA256 `038C672CAB4F6BD9E31E3DC3922FA3657FEABB8491A8F5E522CEA18EE2E79196`；新 release zip `dist/release/ariadne-dev-windows-x64.zip` 为 `16643785` bytes，时间 `2026-06-15 10:35:18`，SHA256 `89E5BD1DBBCDF4291C876523BC9EB8316CD15032682B2FBF431B165ECA3DF4FC`；按用户要求本轮只走后台命令行，未做真实桌面快捷键/截图/任务栏嵌入复验 |
| 设置窗口关闭按钮与快捷键编辑/应用修复并重新打包 | completed | 针对 2026-06-15 用户反馈“快捷键还是不能修改，而且设置窗口没有关闭按钮”：确认设置页原有关闭入口被放在 `.header-tools` ghost 图标内，窄窗口媒体查询会隐藏工具组，改为独立 `window-close-button` 且浅色背景下可见；快捷键输入从只读捕获改为支持手动输入 `alt+q` 文本和点击后按组合键两种方式，新增前端热键规范化/校验、主窗口/截图重复检查、局部“应用快捷键”按钮和运行态注册状态提示；当时贴图快捷键只保存配置，已被 10:46 的“贴图全局快捷键注入”修复覆盖；底部“保存设置”也接入同一校验，避免无效热键写入配置；为规避当前 Windows/P: 盘上 Wails alpha `generate bindings` clean rename `Access is denied`，`Taskfile.yml` 两处 bindings 生成改为 `-clean=false`；按用户要求本轮只走后台命令行，不控制桌面画面或鼠标；`pnpm --dir frontend build`、`go test ./...`、`wails3 task windows:package` 通过；bindings 为 `469 Packages, 23 Services, 181 Methods, 5 Enums, 169 Models, 0 Events`；新 `bin/ariadne.exe` 为 `31768576` bytes，时间 `2026-06-15 10:19:00`，SHA256 `C952131BAE3B626AF3D503EC58CF3A6E29647F0105F2D3F48C8EF0F74C9D4119`；新 release zip `dist/release/ariadne-dev-windows-x64.zip` 为 `16639862` bytes，时间 `2026-06-15 10:19:01`，SHA256 `DAA3AFD3FC2D1464EBC1E012DC447C000E268D0EAC03857054D2BEA3124E67EF`；本项未做真实桌面点击复验 |
| 启动器折叠态白底外框与展开跳动修复并重新打包 | completed | 针对 2026-06-15 用户反馈“白色背景下仍能看到明显边框、初始化位置太中间、输入展开时跳动”：新增 `internal/launcherwindow` 与前端 `launcherGeometry` 共享几何口径，折叠/展开统一使用 `860px` 窗口宽度，折叠高度 `96px`，展开高度 `468px`，窗口初始位置按展开面板高度预留后居中；后端 shell、tool-window launcher bridge、主窗口初始 placement 和前端 appShell/launcher resize 全部移除 launcher 路径上的重复 `Center()`，展开/折叠只改尺寸不改锚点；折叠态 `.palette-shell` 去掉外层背景、边框、阴影和 blur，只保留搜索框本体，避免白底下出现外层壳子轮廓；优先按窗口当前屏幕定位，失败时回落主屏；前后端都用向下取整，避免奇数尺寸屏幕出现 1px 定位差；新增 `TestReservedRelativePositionUsesExpandedLauncherHeight`、小屏 clamp 和折叠/展开同宽测试；按用户要求本轮只走后台命令行，不操作桌面画面或鼠标；`go test ./internal/launcherwindow ./internal/shell ./internal/toolwindows -v`、`go test ./...`、`pnpm build`、`wails3 task windows:build`、`wails3 task windows:package` 通过；bindings 为 `469 Packages, 23 Services, 181 Methods, 5 Enums, 169 Models, 0 Events`；新 `bin/ariadne.exe` 为 `31762432` bytes，时间 `2026-06-15 09:57:54`，SHA256 `71D09DBE9F6EF5DC68B1BB636EA63423FAD397C9D93F1DD4D5264830815383C2`；新 release zip `dist/release/ariadne-dev-windows-x64.zip` 为 `16638203` bytes，时间 `2026-06-15 09:57:55`，SHA256 `04CE7A04F0F48405BF95E68A26AD3E7390FEBD868A96F715C4358EC2DDFB3DDB`；本项未做真实桌面 Alt+Q/输入展开视觉复验 |
| 贴图原生拖动与选区原点对齐修复并重新打包 | completed | 针对 2026-06-15 用户反馈“大小对了但位置又偏、拖动贴图一抖一抖”：确认 JS pointermove 中循环 `Window.Position/SetPosition` 会引入异步 IPC 抖动，贴图主体改用 Wails 原生 `--wails-draggable: drag`，移除 `PinnedImageWindow` 的 JS 拖动状态、pointer capture、move queue 和运行时 `Window.SetPosition` 调用；菜单、OCR 条、OCR 框、按钮等交互区继续 `no-drag`；区域截图创建贴图时去掉旧的 `x-15/y-15`“附近”偏移，改为窗口左上角对齐最终 native 选区左上角；保留 1:1 exact-size、无顶部菜单、默认无阴影、右键菜单临时扩大透明窗口防裁剪；当时曾隐藏 Ariadne 自有非 overlay 窗口，已被 10:35 的“截图不隐藏自身窗口”修复覆盖；新增/更新单测 `TestCaptureSelectionPinsAtSelectionOrigin`、visual resolved origin 和 native offset origin 期望；按用户要求本轮只走后台命令行，不控制鼠标、窗口、快捷键或桌面画面；`go test ./internal/captureoverlay ./internal/pinnedimage -v`、`go test ./...`、`pnpm build`、`wails3 task windows:build`、`wails3 task windows:package` 通过；bindings 为 `468 Packages, 23 Services, 181 Methods, 5 Enums, 169 Models, 0 Events`；新 `bin/ariadne.exe` 为 `31757312` bytes，时间 `2026-06-15 09:33:38`，SHA256 `52C1A03C27DB8B81A72EFBDD17082A12D5A9E0686494046166214F381E021388`；新 release zip `dist/release/ariadne-dev-windows-x64.zip` 为 `16632699` bytes，时间 `2026-06-15 09:33:39`，SHA256 `824954631B0A4580D27A8007611F4DD6FFA48C3EA08070BE3AE10FB4AC5FB147`；本项未做真实桌面贴图位置/拖动/右键菜单复验，MSIX layout 本轮未重建 |
| 开机启动 HKCU Run 非破坏性烟测 | completed | 新增 `internal/shell.RunAutostartSmoke()` 与 `cmd/autostartsmoke`，`wails3 task windows:autostart-smoke` 会写入唯一临时 `HKCU\Software\Microsoft\Windows\CurrentVersion\Run\com.glwlg.ariadne.smoke.*` 值，命令为当前 `ariadne.exe --hidden` 绝对路径，回读后复用 autostart audit 验证 value name、exe 路径和 `--hidden`，最后删除临时值；报告 `dist/perf/ariadne-autostart-smoke-latest.json` 为 `ok=true`、`cleanupOk=true`、`auditValid=true`、`hiddenArgPresent=true`、`commandMatchesExe=true`，PowerShell 复查返回 `CLEANED=...`；`go test ./internal/shell -v`、`go test ./...`、`wails3 task windows:autostart-smoke`、`wails3 task windows:msix` 通过；最新 exe/layout `31762432` bytes，时间 `2026-06-15 09:04:45`，SHA256 `28482F9D19E72C3D29EE8A1E1687A088F415ECF7D87E29CFBA214881D6396D8A`；本项验证注册表写入/回读/清理权限和隐藏启动命令，不等同于真实设置页点击开关 |
| Codex Skill 安装本地发现和刷新握手诊断 | completed | `internal/skills` 新增只读 `InstallDiagnostics`，可扫描 Codex skills 目录、解析已安装 `SKILL.md`、核验 `.ariadne-refresh.json` 与 `.ariadne-refresh.touch` marker 是否一致，并在工作记忆知识面板安装成功后显示本地发现/握手状态；新增安装成功、目标目录缺失、marker 不一致三类测试；`go test ./internal/skills -v`、`go test ./...`、`pnpm build`、`wails3 task windows:build`、`wails3 task windows:msix` 通过；bindings 更新为 `468 Packages, 23 Services, 181 Methods, 5 Enums, 169 Models, 0 Events`；当时验证 exe/layout `31762944` bytes，时间 `2026-06-15 08:56:41`，SHA256 `3EEC2B29CB4E6D566F78A3F853AC13C46A2747BD6ECF8BA726DE39E9E06707A8`；该诊断证明本地 Codex skill 发现目录和 Ariadne refresh marker 成立，但不冒充运行中 Codex 已热加载 |
| 插件参数面板和命令补全真实渲染/交互补齐 | completed | Browser 验证 `生成器` trigger 渲染参数面板，参数 `count=3` 回填 `uuid 3` 并返回 3 条 UUID；主搜索 Enter 回填 `uuid` 并返回 5 条；`pnpm build`、`go test ./...`、`wails3 task windows:build` 通过 |
| 截图选区内容、贴图定位和贴图拖动回归修复 | completed | 选区请求改为前端提交当前 `<img>` 内的 visual 坐标和实际显示尺寸，由 Go 统一映射到 overlay PNG 源像素并裁剪；贴图初始位置不再走前端 `pinX/pinY`，由 Go 使用最终裁剪出的 native 选区换算到 Wails DIP；贴图拖动改为前端运行时 `Window.Position/SetPosition` 直接移动当前窗口，后端 `SetPinnedPosition` 只同步状态，避免 IPC 延迟和旧坐标回拉；新增 visual pin 解析和位置同步测试；`pnpm test:capture-geometry`、`go test ./internal/captureoverlay ./internal/pinnedimage -v`、`go test ./...`、`pnpm build`、`wails3 task windows:build`、`wails3 task windows:msix` 通过；当前 exe/layout `31762432` bytes，时间 `2026-06-15 09:04:45`，SHA256 `28482F9D19E72C3D29EE8A1E1687A088F415ECF7D87E29CFBA214881D6396D8A`；Win32 截图 smoke 在当前 Codex 桌面会话被 `BitBlt 失败: Access is denied.` 阻断，未声明真实桌面内容/贴图/拖动复验 |
| 设置中心安全密钥保存/清除链路补强 | completed | 设置中心 AI / Embedding / Milvus 密钥行新增稳定 `data-secret-*` 测试钩子、保存/清除禁用态和内联结果反馈；保存密钥会清掉旧的清除二次确认状态；Go 测试覆盖三类密钥 trim/save/status/preview clear/confirmed clear，以及旧配置导入后 notes 可见；`go test ./internal/secrets ./internal/settings -v`、`go test ./...`、`pnpm build`、`wails3 task windows:build` 通过；当时验证 exe `31729152` bytes，SHA256 `8A37DB8FA4CF07E68FA1A3B986DF9FED621A273625FBFC0159F18B34F58FAF56`；当前 Computer Use 仍被 `GetCursorPos failed: 拒绝访问 (0x80070005)` 阻断，未声明真实桌面点击烟测 |
| 旧 Python 内置插件 Go 主路径覆盖审计 | completed | `TestLegacyPythonBuiltinsHaveNativeGoCoverage` 枚举 `legacy/x-tools-python/src/plugins/*.py` 并要求 16 个旧内置插件全部映射到 Ariadne 原生 Go manifest 或工具窗口入口；正式替换后旧 Python bridge 已从生产代码删除，该测试只用于防止旧能力遗漏。 |
| unsigned MSIX layout 同步到当前 exe | completed | `wails3 task windows:msix` 通过并重建 `dist/msix/ariadne-0-0-0-0-msix`；layout 内 `Ariadne.exe` 为 `31762432` bytes，时间 `2026-06-15 09:04:45`，SHA256 `28482F9D19E72C3D29EE8A1E1687A088F415ECF7D87E29CFBA214881D6396D8A`；`msix-manifest.json` 记录 `createdAt=1781485485` 且 `packed=false`；`makeappx.exe`、`signtool.exe` 未发现，`C:\Program Files (x86)\Windows Kits\10\bin` 不存在，因此签名 `.msix` 安装/卸载验收仍未完成 |
| 搜索收藏取消空状态清理 | completed | `SetFavorite(false)` 现在会删除既非收藏也无最近使用的空记录，避免设置中心“收藏/最近使用”计数残留；已有最近使用的结果取消收藏后仍保留最近使用、移除收藏 boost/tag，并恢复 `收藏` 动作；新增 `TestSearchUnfavoriteWithoutRecentUseRemovesEmptyRecord` 和 `TestSearchUnfavoriteKeepsRecentUsageButRemovesFavoriteBoost`；`go test ./internal/search -v`、`go test ./...`、`pnpm build`、`wails3 task windows:build`、`wails3 task windows:msix` 通过；当时 exe/layout `31728640` bytes，SHA256 `08A01D300D34CF40C3BE7A66B926F083FD75060B6BAE573178AB3C31A3BE31DC` |
